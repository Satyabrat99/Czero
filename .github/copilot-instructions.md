

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.0 -->
<!-- Updated: 2026-08-12T12:40:44.528Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
api\venv\Lib\site-packages\langgraph_sdk\stream\sync_controller.py ← __future__, langchain_protocol, langgraph_sdk
api\venv\Lib\site-packages\langgraph_sdk\stream\transport\base.py ← __future__, langchain_protocol, httpx
api\venv\Lib\site-packages\langgraph_sdk\stream\transport\http.py ← __future__, langchain_protocol, langgraph_sdk, httpx, orjson
api\venv\Lib\site-packages\langgraph_sdk\stream\transport\sync_http.py ← __future__, langchain_protocol, langgraph_sdk, httpx, orjson
api\venv\Lib\site-packages\langgraph_sdk\stream\transport\sync_ws.py ← __future__, langchain_protocol, websockets, langgraph_sdk, httpx
api\venv\Lib\site-packages\langgraph_sdk\stream\transport\ws.py ← __future__, langchain_protocol, websockets, langgraph_sdk, httpx
api\venv\Lib\site-packages\langgraph\cache\base\__init__.py ← __future__, langgraph
api\venv\Lib\site-packages\langgraph\cache\memory\__init__.py ← __future__, langgraph
api\venv\Lib\site-packages\langgraph\cache\redis\__init__.py ← __future__, langgraph
api\venv\Lib\site-packages\langgraph\checkpoint\base\__init__.py ← __future__, langchain_core, typing_extensions, langgraph
api\venv\Lib\site-packages\langgraph\checkpoint\base\id.py ← __future__
api\venv\Lib\site-packages\langgraph\checkpoint\memory\__init__.py ← __future__, types, langchain_core, langgraph, pickle
api\venv\Lib\site-packages\langgraph\checkpoint\serde\base.py ← __future__
api\venv\Lib\site-packages\langgraph\checkpoint\serde\encrypted.py ← langgraph
api\venv\Lib\site-packages\langgraph\checkpoint\serde\event_hooks.py ← __future__, typing_extensions
api\venv\Lib\site-packages\langgraph\checkpoint\serde\jsonplus.py ← __future__, ipaddress, zoneinfo, langchain_core, langgraph
api\venv\Lib\site-packages\langgraph\checkpoint\serde\types.py ← typing_extensions
api\venv\Lib\site-packages\langgraph\store\base\__init__.py ← __future__, langchain_core, typing_extensions, langgraph
api\venv\Lib\site-packages\langgraph\store\base\batch.py ← __future__, langgraph
api\venv\Lib\site-packages\langgraph\store\base\embed.py ← __future__, langchain_core
api\venv\Lib\site-packages\langgraph\store\memory\__init__.py ← __future__, importlib, langchain_core, langgraph
api\venv\Lib\site-packages\langchain_text_splitters\base.py ← __future__, langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_text_splitters\character.py ← __future__, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\html.py ← __future__, langchain_core, typing_extensions, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\json.py ← __future__, langchain_core
api\venv\Lib\site-packages\langchain_text_splitters\jsx.py ← langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\konlpy.py ← __future__, typing_extensions, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\latex.py ← __future__, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\markdown.py ← __future__, langchain_core, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\nltk.py ← __future__, typing_extensions, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\python.py ← __future__, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\sentence_transformers.py ← __future__, langchain_text_splitters
api\venv\Lib\site-packages\langchain_text_splitters\spacy.py ← __future__, typing_extensions, langchain_text_splitters
api\venv\Lib\site-packages\langchain_openai\chatgpt_oauth.py ← __future__, ipaddress, secrets, webbrowser, httpx
api\venv\Lib\site-packages\langchain_openai\chat_models\_client_utils.py ← __future__, pydantic, httpx, openai
api\venv\Lib\site-packages\langchain_openai\chat_models\azure.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_openai
api\venv\Lib\site-packages\langchain_openai\chat_models\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_openai
api\venv\Lib\site-packages\langchain_openai\chat_models\codex.py ← __future__, langchain_core, pydantic, langchain_openai
api\venv\Lib\site-packages\langchain_openai\embeddings\azure.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_openai
api\venv\Lib\site-packages\langchain_openai\embeddings\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_openai
api\venv\Lib\site-packages\langchain_openai\llms\azure.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_openai
api\venv\Lib\site-packages\langchain_openai\llms\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_openai
api\venv\Lib\site-packages\langchain_openai\middleware\openai_moderation.py ← __future__, langchain, langchain_core, openai
api\venv\Lib\site-packages\langchain_openai\tools\custom_tool.py ← langchain_core
api\venv\Lib\site-packages\langchain_ollama\_utils.py ← __future__, httpx, ollama
api\venv\Lib\site-packages\langchain_ollama\chat_models.py ← __future__, langchain_core, ollama, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_ollama\embeddings.py ← __future__, langchain_core, ollama, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_ollama\llms.py ← __future__, langchain_core, ollama, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_mistralai\_compat.py ← __future__, langchain_core
api\venv\Lib\site-packages\langchain_mistralai\chat_models.py ← __future__, httpx_sse, langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_mistralai\embeddings.py ← httpx, langchain_core, pydantic, tenacity, tokenizers
api\venv\Lib\site-packages\langchain_aws\__init__.py ← langchain_aws
api\venv\Lib\site-packages\langchain_aws\function_calling.py ← langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_aws\utils.py ← botocore, langchain_core, packaging, pydantic
api\venv\Lib\site-packages\langchain_aws\agents\base.py ← __future__, botocore, langchain_core, pydantic, langchain_aws
api\venv\Lib\site-packages\langchain_aws\agents\types.py ← __future__, langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_aws\agents\utils.py ← __future__, botocore, langchain_core, langchain_aws, boto3
api\venv\Lib\site-packages\langchain_aws\chains\graph_qa\neptune_cypher.py ← __future__, langchain_core, langchain_aws, prompts
api\venv\Lib\site-packages\langchain_aws\chains\graph_qa\neptune_sparql.py ← __future__, langchain_core, langchain_aws, prompts
api\venv\Lib\site-packages\langchain_aws\chat_models\anthropic.py ← anthropic, langchain_anthropic, langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock.py ← langchain_core, pydantic, langchain_aws
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py ← botocore, langchain_core, pydantic, typing_extensions, langchain_aws
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_nova_sonic.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_aws
api\venv\Lib\site-packages\langchain_aws\chat_models\sagemaker_endpoint.py ← langchain_core, pydantic, typing_extensions, langchain_aws
api\venv\Lib\site-packages\langchain_aws\document_compressors\rerank.py ← langchain_core, pydantic, langchain_aws
api\venv\Lib\site-packages\langchain_aws\embeddings\bedrock.py ← langchain_core, pydantic, typing_extensions, langchain_aws, numpy
api\venv\Lib\site-packages\langchain_aws\graphs\neptune_graph.py ← pydantic, langchain_aws
api\venv\Lib\site-packages\langchain_aws\graphs\neptune_rdf_graph.py ← types, requests
api\venv\Lib\site-packages\langchain_aws\llms\bedrock.py ← langchain_core, pydantic, typing_extensions, langchain_aws
api\venv\Lib\site-packages\langchain_aws\llms\sagemaker_endpoint.py ← langchain_core, pydantic, typing_extensions, langchain_aws
api\venv\Lib\site-packages\langchain_aws\middleware\prompt_caching.py ← langchain_aws
api\venv\Lib\site-packages\langchain_aws\retrievers\bedrock.py ← botocore, langchain_core, pydantic, typing_extensions, langchain_aws
api\venv\Lib\site-packages\langchain_aws\retrievers\kendra.py ← langchain_core, pydantic, typing_extensions, langchain_aws
api\venv\Lib\site-packages\langchain_aws\retrievers\s3_vectors.py ← langchain_core
api\venv\Lib\site-packages\langchain_aws\runnables\q_business.py ← langchain_core, pydantic
api\venv\Lib\site-packages\langchain_aws\tools\browser_session_manager.py ← __future__, bedrock_agentcore
api\venv\Lib\site-packages\langchain_aws\tools\browser_toolkit.py ← bedrock_agentcore, langchain_core, browser_session_manager, browser_tools
api\venv\Lib\site-packages\langchain_aws\tools\browser_tools.py ← langchain_core, pydantic, browser_session_manager, utils
api\venv\Lib\site-packages\langchain_aws\tools\code_interpreter_toolkit.py ← bedrock_agentcore, langchain_core, pydantic, utils
api\venv\Lib\site-packages\langchain_aws\tools\nova_tools.py ← langchain_core, pydantic
api\venv\Lib\site-packages\langchain_aws\tools\utils.py ← __future__, langchain_core
api\venv\Lib\site-packages\langchain_aws\utilities\math.py ← numpy
api\venv\Lib\site-packages\langchain_aws\utilities\redis.py ← __future__, numpy
api\venv\Lib\site-packages\langchain_aws\utilities\utils.py ← langchain_core, langchain_aws, numpy
api\venv\Lib\site-packages\langchain_aws\utilities\valkey.py ← __future__
api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\base.py ← __future__, langchain_core, pydantic, langchain_aws, numpy
api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\cache.py ← __future__, langchain_core, langchain_aws
api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\filters.py ← langchain_aws
api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\schema.py ← __future__, pydantic, typing_extensions, langchain_aws, numpy
api\venv\Lib\site-packages\langchain_aws\vectorstores\s3_vectors\base.py ← __future__, botocore, langchain_core, pydantic, langchain_aws
api\venv\Lib\site-packages\langchain_aws\vectorstores\valkey\base.py ← __future__, langchain_core, pydantic, langchain_aws, numpy
api\venv\Lib\site-packages\langgraph\prebuilt\_tool_call_stream.py ← __future__, langgraph
api\venv\Lib\site-packages\langgraph\prebuilt\_tool_call_transformer.py ← __future__, langgraph
api\venv\Lib\site-packages\langgraph\prebuilt\chat_agent_executor.py ← langchain_core, langgraph, pydantic, typing_extensions
api\venv\Lib\site-packages\langgraph\prebuilt\interrupt.py ← langgraph, typing_extensions
api\venv\Lib\site-packages\langgraph\prebuilt\tool_node.py ← __future__, types, langchain_core, langgraph, pydantic
api\venv\Lib\site-packages\langgraph\prebuilt\tool_validator.py ← langchain_core, langgraph, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\base_memory.py ← __future__, langchain_core, pydantic
api\venv\Lib\site-packages\langchain_classic\env.py ← platform
api\venv\Lib\site-packages\langchain_classic\hub.py ← __future__, langchain_core, langsmith
api\venv\Lib\site-packages\langchain_classic\model_laboratory.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\_api\module_import.py ← langchain_core, langchain_classic, importlib
api\venv\Lib\site-packages\langchain_classic\agents\agent.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\agent_iterator.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\agent_types.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\initialize.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\loading.py ← langchain_core, langchain_classic, yaml
api\venv\Lib\site-packages\langchain_classic\agents\schema.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\agents\tools.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\agents\utils.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\agents\chat\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\chat\output_parser.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\conversational\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\conversational\output_parser.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\conversational_chat\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\conversational_chat\output_parser.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\log.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\log_to_messages.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\openai_functions.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\tools.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\xml.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\agents\json_chat\base.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\mrkl\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\mrkl\output_parser.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\openai_assistant\base.py ← __future__, langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\agents\openai_functions_agent\agent_token_buffer_memory.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\openai_functions_agent\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\openai_functions_multi_agent\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\openai_tools\base.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\json.py ← __future__, langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\openai_functions.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\openai_tools.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\react_json_single_input.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\react_single_input.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\self_ask.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\tools.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\xml.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\react\agent.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\react\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\react\output_parser.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\self_ask_with_search\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\structured_chat\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\structured_chat\output_parser.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\tool_calling_agent\base.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\agents\xml\base.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\callbacks\streaming_aiter_final_only.py ← __future__, langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\callbacks\streaming_aiter.py ← __future__, langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\callbacks\streaming_stdout_final_only.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\callbacks\streamlit\__init__.py ← __future__, langchain_core
api\venv\Lib\site-packages\langchain_classic\callbacks\tracers\logging.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\chains\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic, yaml
api\venv\Lib\site-packages\langchain_classic\chains\example_generator.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\chains\history_aware_retriever.py ← __future__, langchain_core
api\venv\Lib\site-packages\langchain_classic\chains\llm.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\loading.py ← __future__, langchain_core, langchain_classic, yaml
api\venv\Lib\site-packages\langchain_classic\chains\mapreduce.py ← __future__, langchain_core, langchain_text_splitters, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\moderation.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\prompt_selector.py ← langchain_core, pydantic
api\venv\Lib\site-packages\langchain_classic\chains\retrieval.py ← __future__, langchain_core
api\venv\Lib\site-packages\langchain_classic\chains\sequential.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\transform.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\base.py ← langchain_core, langchain_text_splitters, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\map_reduce.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\map_rerank.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\reduce.py ← __future__, langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\refine.py ← __future__, langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\stuff.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\constitutional_ai\base.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\constitutional_ai\models.py ← pydantic
api\venv\Lib\site-packages\langchain_classic\chains\conversation\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\conversational_retrieval\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\elasticsearch_database\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\flare\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\flare\prompts.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\chains\hyde\base.py ← __future__, langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\llm_checker\base.py ← __future__, langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\llm_math\base.py ← __future__, langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\llm_summarization_checker\base.py ← __future__, langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\natbot\base.py ← __future__, langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\base.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\citation_fuzzy_match.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\extraction.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\openapi.py ← __future__, langchain_core, requests, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\qa_with_structure.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\tagging.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\openai_tools\extraction.py ← langchain_core, pydantic
api\venv\Lib\site-packages\langchain_classic\chains\qa_generation\base.py ← __future__, langchain_core, langchain_text_splitters, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\loading.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\retrieval.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\vector_db.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\query_constructor\base.py ← __future__, langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\query_constructor\parser.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\chains\query_constructor\schema.py ← pydantic
api\venv\Lib\site-packages\langchain_classic\chains\question_answering\chain.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\retrieval_qa\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\router\base.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\router\embedding_router.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\router\llm_router.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\router\multi_prompt.py ← __future__, langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\router\multi_retrieval_qa.py ← __future__, langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\sql_database\query.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chains\structured_output\base.py ← langchain_core, pydantic
api\venv\Lib\site-packages\langchain_classic\chains\summarize\chain.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\chat_models\base.py ← __future__, importlib, langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\embeddings\base.py ← importlib, langchain_core
api\venv\Lib\site-packages\langchain_classic\embeddings\cache.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\loading.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\schema.py ← __future__, langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\agents\trajectory_eval_chain.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\comparison\eval_chain.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\criteria\eval_chain.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\embedding_distance\base.py ← importlib, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\exact_match\base.py ← typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\parsing\base.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\parsing\json_distance.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\parsing\json_schema.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\qa\eval_chain.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\qa\generate_chain.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\regex_match\base.py ← typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\scoring\eval_chain.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\evaluation\string_distance\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\indexes\_sql_record_manager.py ← langchain_core, sqlalchemy
api\venv\Lib\site-packages\langchain_classic\indexes\vectorstore.py ← langchain_core, langchain_text_splitters, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\llms\__init__.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\buffer.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\buffer_window.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\chat_memory.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\combined.py ← pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\entity.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\readonly.py ← langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\simple.py ← typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\summary.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\summary_buffer.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\token_buffer.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\vectorstore.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\memory\vectorstore_token_buffer_memory.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\output_parsers\boolean.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\output_parsers\combining.py ← __future__, langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\output_parsers\datetime.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\output_parsers\enum.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\output_parsers\fix.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\output_parsers\loading.py ← langchain_classic
api\venv\Lib\site-packages\langchain_classic\output_parsers\pandas_dataframe.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\output_parsers\regex.py ← __future__, langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\output_parsers\retry.py ← __future__, langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\output_parsers\regex_dict.py ← __future__, langchain_core
api\venv\Lib\site-packages\langchain_classic\output_parsers\structured.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\output_parsers\yaml.py ← langchain_core, pydantic, typing_extensions, langchain_classic, yaml
api\venv\Lib\site-packages\langchain_classic\retrievers\contextual_compression.py ← langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\retrievers\ensemble.py ← langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\retrievers\merger_retriever.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\retrievers\multi_query.py ← langchain_core, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\retrievers\multi_vector.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\retrievers\parent_document_retriever.py ← langchain_core, langchain_text_splitters, langchain_classic
api\venv\Lib\site-packages\langchain_classic\retrievers\re_phraser.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\retrievers\time_weighted_retriever.py ← langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\base.py ← langchain_core, pydantic
api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\chain_extract.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\chain_filter.py ← langchain_core, pydantic, langchain_classic
api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\cohere_rerank.py ← __future__, langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\cross_encoder_rerank.py ← __future__, langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\embeddings_filter.py ← langchain_core, pydantic, typing_extensions
api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\listwise_rerank.py ← langchain_core, pydantic
api\venv\Lib\site-packages\langchain_classic\retrievers\self_query\base.py ← langchain_core, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\runnables\hub.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\runnables\openai_functions.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\smith\evaluation\config.py ← langchain_core, langsmith, pydantic, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\smith\evaluation\progress.py ← langchain_core, typing_extensions
api\venv\Lib\site-packages\langchain_classic\smith\evaluation\runner_utils.py ← __future__, langchain_core, langsmith, requests, typing_extensions
api\venv\Lib\site-packages\langchain_classic\smith\evaluation\string_run_evaluator.py ← __future__, langchain_core, langsmith, typing_extensions, langchain_classic
api\venv\Lib\site-packages\langchain_classic\storage\_lc_store.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\storage\encoder_backed.py ← langchain_core
api\venv\Lib\site-packages\langchain_classic\storage\file_system.py ← langchain_core, langchain_classic
api\venv\Lib\site-packages\langchain_classic\utils\pydantic.py ← langchain_core
api\venv\Lib\site-packages\feedparser_sgmllib\__init__.py ← _compat, _markupbase
api\venv\Lib\site-packages\feedparser\api.py ← datetimes, encodings, exceptions, html, .
api\venv\Lib\site-packages\feedparser\encodings.py ← exceptions
api\venv\Lib\site-packages\feedparser\html.py ← sgml
api\venv\Lib\site-packages\feedparser\http.py ← datetimes, urls, gzip, zlib
api\venv\Lib\site-packages\feedparser\mixin.py ← html, namespaces, sanitizer, util, urls
api\venv\Lib\site-packages\feedparser\sanitizer.py ← html, urls
api\venv\Lib\site-packages\feedparser\sgml.py ← feedparser_sgmllib
api\venv\Lib\site-packages\feedparser\urls.py ← html
api\venv\Lib\site-packages\feedparser\datetimes\__init__.py ← asctime, greek, hungarian, iso8601, korean
api\venv\Lib\site-packages\feedparser\namespaces\_base.py ← datetimes, urls, util
api\venv\Lib\site-packages\feedparser\namespaces\admin.py ← util
api\venv\Lib\site-packages\feedparser\namespaces\cc.py ← util
api\venv\Lib\site-packages\feedparser\namespaces\dc.py ← datetimes, util
api\venv\Lib\site-packages\feedparser\namespaces\georss.py ← __future__, util
api\venv\Lib\site-packages\feedparser\namespaces\itunes.py ← util
api\venv\Lib\site-packages\feedparser\namespaces\mediarss.py ← util
api\venv\Lib\site-packages\feedparser\namespaces\psc.py ← ..
api\venv\Lib\site-packages\feedparser\parsers\strict.py ← exceptions
api\app\services\scraper.py ← exa_py, httpx
api\test_pipeline_nvidia.py ← dotenv, app
api\app\config.py ← pydantic_settings
api\test_scheduler.py ← unittest, dotenv, app
api\app\engines\collector\sources\hn.py ← base, httpx
api\test_prompt_relevance.py ← dotenv, app
api\app\services\analyzer.py ← openai
api\app\services\db.py ← supabase, app
api\app\workers\scheduler.py ← app
api\app\engines\collector\orchestrator.py ← sources, merger
api\app\engines\collector\sources\devto.py ← base, httpx
api\app\engines\collector\sources\exa.py ← exa_py, base
api\app\engines\collector\sources\exa_producthunt.py ← exa_py, base
api\app\engines\collector\sources\exa_quora.py ← exa_py, base
api\app\engines\collector\sources\exa_reddit.py ← exa_py, base
api\app\engines\collector\sources\exa_twitter.py ← exa_py, base
api\app\engines\collector\sources\indiehackers.py ← base, httpx
api\app\engines\collector\sources\lobsters.py ← base, httpx
api\app\engines\collector\sources\reddit_rss.py ← base, feedparser, httpx
api\app\engines\scorer\llm_scorer.py ← openai
api\app\main.py ← dotenv, fastapi, app, pydantic
api\app\routes\billing.py ← fastapi, pydantic
api\app\routes\products.py ← fastapi, pydantic, app
api\app\services\drafter.py ← openai
```

## todos
```
api\venv\Lib\site-packages\langchain_openai\chat_models\_client_utils.py:381  # TODO: (someday): support non asyncio runtimes here
api\venv\Lib\site-packages\langchain_openai\chat_models\azure.py:487  # TODO: Remove OPENAI_API_KEY support to avoid possible conflict when using
api\venv\Lib\site-packages\langchain_openai\chat_models\azure.py:660  # XXX: .openai.azure.com/", '
api\venv\Lib\site-packages\langchain_openai\chat_models\azure.py:663  # XXX: .openai.azure.com/openai/deployments/my-deployment"'
api\venv\Lib\site-packages\langchain_openai\chat_models\base.py:2135  # TODO: Count bound tools as part of input.
api\venv\Lib\site-packages\langchain_openai\embeddings\azure.py:117  # TODO: Remove OPENAI_API_KEY support to avoid possible conflict when using
api\venv\Lib\site-packages\langchain_openai\embeddings\base.py:198  # TODO: Move to AzureOpenAIEmbeddings.
api\venv\Lib\site-packages\langchain_openai\llms\azure.py:62  # TODO: Remove OPENAI_API_KEY support to avoid possible conflict when using
api\venv\Lib\site-packages\langchain_openai\llms\base.py:469  # TODO: write a unit test for this
api\venv\Lib\site-packages\langchain_mistralai\chat_models.py:721  # TODO: handle retries
api\venv\Lib\site-packages\langchain_mistralai\chat_models.py:739  # TODO: handle retries and max_concurrency
api\venv\Lib\site-packages\langchain_mistralai\chat_models.py:1197  # TODO: Update to pass in tool name as tool_choice if/when Mistral supports
api\venv\Lib\site-packages\langchain_mistralai\embeddings.py:181  # TODO: handle retries
api\venv\Lib\site-packages\langchain_mistralai\embeddings.py:192  # TODO: handle retries and max_concurrency
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py:1373  # TODO: Add async support once there are async bedrock.converse methods.
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py:1564  # TODO: remove restriction to thinking Claude models. If a model does not
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py:2342  # TODO: needed?
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py:2393  # TODO: needed?
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py:2396  # TODO: snake case response metadata?
api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py:2589  # TODO: restore search result citations once fixed on Bedrock side
```

## api

### api\venv\Lib\site-packages\langgraph_sdk\stream\sync_controller.py
```
@dataclass _SyncSubscription(id, params, queue)
class SyncStreamController
  def register_subscription(params: SubscribeParams) → _SyncSubscription
  def unregister_subscription(subscription_id: int) → None
  def signal_paused() → None
  def reconcile_stream(candidate_filter: SubscribeParams) → None
  def ensure_fanout_running() → None
  def observe_applied_through_seq(seq: Any) → None
  def close() → None
```

### api\venv\Lib\site-packages\langgraph_sdk\stream\transport\base.py
```
@dataclass EventStreamHandle(events, ready, done, close)
@dataclass SyncEventStreamHandle(events, error, close)
class AsyncProtocolTransport(Protocol)
  async def send_command(command: dict[str, Any]) → dict[str, Any] | None
  def open_event_stream(params: dict[str, Any]) → EventStreamHandle
  async def close() → None
class SyncProtocolTransport(Protocol)
  def send_command(command: dict[str, Any]) → dict[str, Any] | None
  def open_event_stream(params: dict[str, Any]) → SyncEventStreamHandle
  def close() → None
def build_event_stream_body(params: dict[str, Any]) → dict[str, Any]
def build_websocket_url(base_url: httpx.URL, path: str) → str  # Convert an HTTP base URL plus API path into a WebSocket URL
def websocket_headers(headers: Mapping[str, str] | None) → list[tuple[str, str]]
```

### api\venv\Lib\site-packages\langgraph_sdk\stream\transport\http.py
```
class ProtocolSseTransport
  async def send_command(command: dict[str, Any]) → dict[str, Any] | None
  def open_event_stream(params: dict[str, Any]) → EventStreamHandle
  async def pump() → None
  async def aiter() → AsyncIterator[Event]
  async def close() → None
  async def close() → None
```

### api\venv\Lib\site-packages\langgraph_sdk\stream\transport\sync_http.py
```
class SyncProtocolSseTransport
  def send_command(command: dict[str, Any]) → dict[str, Any] | None
  def open_event_stream(params: dict[str, Any]) → SyncEventStreamHandle
  def events() → Iterator[Event]
  def error() → BaseException | None
  def close() → None
  def close() → None
```

### api\venv\Lib\site-packages\langgraph_sdk\stream\transport\sync_ws.py
```
class SyncProtocolWebSocketTransport
  def send_command(command: dict[str, Any]) → dict[str, Any] | None
  def open_event_stream(params: dict[str, Any]) → SyncEventStreamHandle
  def events() → Iterator[Event]
  def error() → BaseException | None
  def close() → None
  def close() → None
```

### api\venv\Lib\site-packages\langgraph_sdk\stream\transport\ws.py
```
class ProtocolWebSocketTransport
  async def send_command(command: dict[str, Any]) → dict[str, Any] | None
  def open_event_stream(params: dict[str, Any]) → EventStreamHandle
  async def pump() → None
  async def aiter() → AsyncIterator[Event]
  async def close() → None
  async def close() → None
```

### api\venv\Lib\site-packages\langgraph\cache\base\__init__.py
```
class BaseCache(ABC, Generic[ValueT])
  def __init__(*, serde: SerializerProtocol | None) → None
  def get(keys: Sequence[FullKey]) → dict[FullKey, ValueT]
  async def aget(keys: Sequence[FullKey]) → dict[FullKey, ValueT]
  def set(pairs: Mapping[FullKey, tuple[ValueT, int | None]]) → None
  async def aset(pairs: Mapping[FullKey, tuple[ValueT, int | None]]) → None
  def clear(namespaces: Sequence[Namespace] | None) → None
  async def aclear(namespaces: Sequence[Namespace] | None) → None
```

### api\venv\Lib\site-packages\langgraph\cache\memory\__init__.py
```
class InMemoryCache(BaseCache[ValueT])
  def __init__(*, serde: SerializerProtocol | None)
  def get(keys: Sequence[FullKey]) → dict[FullKey, ValueT]
  async def aget(keys: Sequence[FullKey]) → dict[FullKey, ValueT]
  def set(keys: Mapping[FullKey, tuple[ValueT, int | None]]) → None
  async def aset(keys: Mapping[FullKey, tuple[ValueT, int | None]]) → None
  def clear(namespaces: Sequence[Namespace] | None) → None
  async def aclear(namespaces: Sequence[Namespace] | None) → None
```

### api\venv\Lib\site-packages\langgraph\cache\redis\__init__.py
```
class RedisCache(BaseCache[ValueT])
  def get(keys: Sequence[FullKey]) → dict[FullKey, ValueT]
  async def aget(keys: Sequence[FullKey]) → dict[FullKey, ValueT]
  def set(mapping: Mapping[FullKey, tuple[ValueT, int | None]]) → None
  async def aset(mapping: Mapping[FullKey, tuple[ValueT, int | None]]) → None
  def clear(namespaces: Sequence[Namespace] | None) → None
  async def aclear(namespaces: Sequence[Namespace] | None) → None
```

### api\venv\Lib\site-packages\langgraph\checkpoint\base\__init__.py
```
class CheckpointMetadata(TypedDict, total=False)
class Checkpoint(TypedDict)
class CheckpointTuple(NamedTuple)
class DeltaChannelHistory(TypedDict)
class BaseCheckpointSaver(Generic[V])
  def config_specs() → list
  def get(config: RunnableConfig) → Checkpoint | None
  def get_tuple(config: RunnableConfig) → CheckpointTuple | None
  async def aget(config: RunnableConfig) → Checkpoint | None
  async def aget_tuple(config: RunnableConfig) → CheckpointTuple | None
  def get_next_version(current: V | None, channel: None) → V
class EmptyChannelError(Exception)
def copy_checkpoint(checkpoint: Checkpoint) → Checkpoint
def get_checkpoint_id(config: RunnableConfig) → str | None  # Get checkpoint ID
def get_checkpoint_metadata(config: RunnableConfig, metadata: CheckpointMetadata) → CheckpointMetadata  # Get checkpoint metadata in a backwards-compatible manner
def get_serializable_checkpoint_metadata(config: RunnableConfig, metadata: CheckpointMetadata) → CheckpointMetadata  # Get checkpoint metadata in a backwards-compatible manner
def empty_checkpoint() → Checkpoint
def create_checkpoint(checkpoint: Checkpoint, channels: Mapping[str, ChannelProtocol] | None, step: int, *, id: str | None) → Checkpoint  # Create a checkpoint for the given channels
```

### api\venv\Lib\site-packages\langgraph\checkpoint\base\id.py
```
class UUID(uuid.UUID)
  def subsec() → int
  def time() → int
def uuid6(node: int | None, clock_seq: int | None) → UUID  # UUID version 6 is a field-compatible version of UUIDv1, reor
```

### api\venv\Lib\site-packages\langgraph\checkpoint\memory\__init__.py
```
class InMemorySaver(BaseCheckpointSaver[str], AbstractContextManager, AbstractAsyncContextManager)
  def get_tuple(config: RunnableConfig) → CheckpointTuple | None
  def delete_thread(thread_id: str) → None
  async def aget_tuple(config: RunnableConfig) → CheckpointTuple | None
  async def adelete_thread(thread_id: str) → None
  def get_next_version(current: str | None, channel: None) → str
class PersistentDict(defaultdict)
  def __init__(*args: Any, filename: str, **kwds: Any) → None
  def sync() → None
  def close() → None
  def dump(fileobj: Any) → None
  def load() → None
```

### api\venv\Lib\site-packages\langgraph\checkpoint\serde\base.py
```
class UntypedSerializerProtocol(Protocol)
  def dumps(obj: Any) → bytes
  def loads(data: bytes) → Any
class SerializerProtocol(Protocol)
  def dumps_typed(obj: Any) → tuple[str, bytes]
  def loads_typed(data: tuple[str, bytes]) → Any
class SerializerCompat(SerializerProtocol)
  def __init__(serde: UntypedSerializerProtocol) → None
  def dumps_typed(obj: Any) → tuple[str, bytes]
  def loads_typed(data: tuple[str, bytes]) → Any
class CipherProtocol(Protocol)
  def encrypt(plaintext: bytes) → tuple[str, bytes]
  def decrypt(ciphername: str, ciphertext: bytes) → bytes
def maybe_add_typed_methods(serde: SerializerProtocol | UntypedSerializerProtocol) → SerializerProtocol  # Wrap serde old serde implementations in a class with loads_t
```

### api\venv\Lib\site-packages\langgraph\checkpoint\serde\encrypted.py
```
class EncryptedSerializer(SerializerProtocol)
  def dumps_typed(obj: Any) → tuple[str, bytes]
  def loads_typed(data: tuple[str, bytes]) → Any
  def encrypt(plaintext: bytes) → tuple[str, bytes]
  def decrypt(ciphername: str, ciphertext: bytes) → bytes
```

### api\venv\Lib\site-packages\langgraph\checkpoint\serde\event_hooks.py
```
class SerdeEvent(TypedDict)
def register_serde_event_listener(listener: SerdeEventListener) → Callable[[], None]  # Register a listener for serde allowlist events
def emit_serde_event(event: SerdeEvent) → None  # Emit a serde event to all listeners
```

### api\venv\Lib\site-packages\langgraph\checkpoint\serde\jsonplus.py
```
class JsonPlusSerializer(SerializerProtocol)
  def dumps_typed(obj: Any) → tuple[str, bytes]
  def loads_typed(data: tuple[str, bytes]) → Any
class InvalidModuleError(Exception)
  def __init__(message: str)
```

### api\venv\Lib\site-packages\langgraph\checkpoint\serde\types.py
```
class _DeltaSnapshot(NamedTuple)
class ChannelProtocol(Protocol[Value, Update, C])
  def ValueType() → Any
  def UpdateType() → Any
  def checkpoint() → C | None
  def from_checkpoint(checkpoint: C | None) → Self
  def update(values: Sequence[Update]) → bool
  def get() → Value
  def consume() → bool
class SendProtocol(Protocol)
```

### api\venv\Lib\site-packages\langgraph\store\base\__init__.py
```
class NotProvided
class Item
  def dict() → dict
class SearchItem(Item)
  def dict() → dict
class GetOp(NamedTuple)
class SearchOp(NamedTuple)
class MatchCondition(NamedTuple)
class ListNamespacesOp(NamedTuple)
class PutOp(NamedTuple)
class InvalidNamespaceError(ValueError)
class TTLConfig(TypedDict, total=False)
class IndexConfig(TypedDict, total=False)
class BaseStore(ABC)
  def batch(ops: Iterable[Op]) → list[Result]
  async def abatch(ops: Iterable[Op]) → list[Result]
  def delete(namespace: tuple[str, ...], key: str) → None
  async def adelete(namespace: tuple[str, ...], key: str) → None
```

### api\venv\Lib\site-packages\langgraph\store\base\batch.py
```
class AsyncBatchedBaseStore(BaseStore)
  def __init__() → None
  def batch(ops: Iterable[Op]) → list[Result]
```

### api\venv\Lib\site-packages\langgraph\store\base\embed.py
```
class EmbeddingsLambda(Embeddings)
  def embed_documents(texts: list[str]) → list[list[float]]
  def embed_query(text: str) → list[float]
  async def aembed_documents(texts: list[str]) → list[list[float]]
  async def aembed_query(text: str) → list[float]
def ensure_embeddings(embed: Embeddings | EmbeddingsFunc | AEmbeddingsFunc | str | None) → Embeddings
def get_text_at_path(obj: Any, path: str | list[str]) → list[str]  # Extract text from an object using a path expression or pre-t
def tokenize_path(path: str) → list[str]  # Tokenize a path into components
```

### api\venv\Lib\site-packages\langgraph\store\memory\__init__.py
```
class InMemoryStore(BaseStore)
  def __init__(*, index: IndexConfig | None) → None
  def batch(ops: Iterable[Op]) → list[Result]
  async def abatch(ops: Iterable[Op]) → list[Result]
  def filter_func(item: Item) → bool
```

### api\venv\Lib\site-packages\langchain_text_splitters\base.py
```
class TextSplitter(BaseDocumentTransformer, ABC)
  def split_text(text: str) → list[str]
  def split_documents(documents: Iterable[Document]) → list[Document]
class TokenTextSplitter(TextSplitter)
  def split_text(text: str) → list[str]
class Language(str, Enum)
@dataclass Tokenizer(chunk_overlap, tokens_per_chunk, decode, encode)
def split_text_on_tokens(*, text: str, tokenizer: Tokenizer) → list[str]  # Split incoming text and return chunks using tokenizer
```

### api\venv\Lib\site-packages\langchain_text_splitters\character.py
```
class CharacterTextSplitter(TextSplitter)
  def split_text(text: str) → list[str]
class RecursiveCharacterTextSplitter(TextSplitter)
  def split_text(text: str) → list[str]
  def get_separators_for_language(language: Language) → list[str]
```

### api\venv\Lib\site-packages\langchain_text_splitters\html.py
```
class ElementType(TypedDict)
class HTMLHeaderTextSplitter
  def split_text(text: str) → list[Document]
  def split_text_from_file(file: str | IO[str]) → list[Document]
  def finalize_chunk() → Document | None
class HTMLSectionSplitter
  def split_documents(documents: Iterable[Document]) → list[Document]
  def split_text(text: str) → list[Document]
  def split_html_by_headers(html_doc: str) → list[dict[str, str | None]]
  def convert_possible_tags_to_header(html_content: str) → str
  def split_text_from_file(file: StringIO) → list[Document]
class HTMLSemanticPreservingSplitter(BaseDocumentTransformer)
  def split_text(text: str) → list[Document]
```

### api\venv\Lib\site-packages\langchain_text_splitters\json.py
```
class RecursiveJsonSplitter
```

### api\venv\Lib\site-packages\langchain_text_splitters\jsx.py
```
class JSFrameworkTextSplitter(RecursiveCharacterTextSplitter)
  def split_text(text: str) → list[str]
```

### api\venv\Lib\site-packages\langchain_text_splitters\konlpy.py
```
class KonlpyTextSplitter(TextSplitter)
  def split_text(text: str) → list[str]
```

### api\venv\Lib\site-packages\langchain_text_splitters\latex.py
```
class LatexTextSplitter(RecursiveCharacterTextSplitter)
  def __init__(**kwargs: Any) → None
```

### api\venv\Lib\site-packages\langchain_text_splitters\markdown.py
```
class MarkdownTextSplitter(RecursiveCharacterTextSplitter)
  def __init__(**kwargs: Any) → None
class MarkdownHeaderTextSplitter
  def aggregate_lines_to_chunks(lines: list[LineType]) → list[Document]
  def split_text(text: str) → list[Document]
class LineType(TypedDict)
class HeaderType(TypedDict)
class ExperimentalMarkdownSyntaxTextSplitter
  def split_text(text: str) → list[Document]
```

### api\venv\Lib\site-packages\langchain_text_splitters\nltk.py
```
class NLTKTextSplitter(TextSplitter)
  def split_text(text: str) → list[str]
```

### api\venv\Lib\site-packages\langchain_text_splitters\python.py
```
class PythonCodeTextSplitter(RecursiveCharacterTextSplitter)
  def __init__(**kwargs: Any) → None
```

### api\venv\Lib\site-packages\langchain_text_splitters\sentence_transformers.py
```
class SentenceTransformersTokenTextSplitter(TextSplitter)
  def split_text(text: str) → list[str]
  def encode_strip_start_and_stop_token_ids(text: str) → list[int]
  def count_tokens(*, text: str) → int
```

### api\venv\Lib\site-packages\langchain_text_splitters\spacy.py
```
class SpacyTextSplitter(TextSplitter)
  def split_text(text: str) → list[str]
```

### api\venv\Lib\site-packages\langchain_openai\chatgpt_oauth.py
```
@dataclass _ChatGPTToken(access_token, refresh_token, expires_at, account_id?, plan_type?, user_id?, id_token?)
class _ChatGPTOAuthRefreshError(RuntimeError)
class _ChatGPTOAuthTokenProvider(Protocol)
  def get_token() → _ChatGPTToken
  async def aget_token() → _ChatGPTToken
  def get_access_token() → str
  async def aget_access_token() → str
@dataclass _FileChatGPTOAuthTokenProvider(path, client_id, token_url, refresh_skew, timeout, _cached?, _lock)
class _CallbackHandler(http.server.BaseHTTPRequestHandler)
  def do_GET() → None
  def log_message(format: str, *args: Any) → None
def decode_jwt_claims(token: str) → dict[str, Any]  # Decode a JWT's payload without signature verification
def login_chatgpt(*, store_path: Path | None, client_id: str, host: str, port: int, callback_path: str, scope: str, open_browser: bool, timeout: float) → _FileChatGPTOAuthTokenProvider
def login_chatgpt_device(*, store_path: Path | None, client_id: str, poll_interval: float, timeout: float) → _FileChatGPTOAuthTokenProvider
```

### api\venv\Lib\site-packages\langchain_openai\chat_models\_client_utils.py
```
class _SyncHttpxClientWrapper(openai.DefaultHttpxClient)
class _AsyncHttpxClientWrapper(openai.DefaultAsyncHttpxClient)
class StreamChunkTimeoutError(*_StreamChunkTimeoutBases)
```

### api\venv\Lib\site-packages\langchain_openai\chat_models\azure.py
```
class AzureChatOpenAI(BaseChatOpenAI)
  def get_lc_namespace() → list[str]
  def lc_secrets() → dict[str, str]
  def is_lc_serializable() → bool
  def validate_environment() → Self
  def lc_attributes() → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_openai\chat_models\base.py
```
class OpenAIContextOverflowError(openai.BadRequestError, ContextOverflowError)
class OpenAIAPIContextOverflowError(openai.APIError, ContextOverflowError)
class BaseChatOpenAI(BaseChatModel)
  def model() → str
  def build_extra(values: dict[str, Any]) → Any
  def validate_temperature(values: dict[str, Any]) → Any
  def validate_environment() → Self
  def get_token_ids(text: str) → list[int]
class ChatOpenAI(BaseChatOpenAI)
  def lc_secrets() → dict[str, str]
  def get_lc_namespace() → list[str]
  def lc_attributes() → dict[str, Any]
  def is_lc_serializable() → bool
class OpenAIRefusalError(Exception)
```

### api\venv\Lib\site-packages\langchain_openai\chat_models\codex.py
```
class _ChatOpenAICodex(ChatOpenAI)
  def is_lc_serializable() → bool
class _SyncTokenCallable
  def __init__(provider: _ChatGPTOAuthTokenProvider) → None
```

### api\venv\Lib\site-packages\langchain_openai\data\profile_augmentations.toml
```
table [overrides]
table [overrides."gpt-3.5-turbo"]
table [overrides."gpt-5.1-codex"]
table [overrides."gpt-5.2-pro"]
table [overrides."gpt-5.1-codex-mini"]
table [overrides."gpt-5.2-chat-latest"]
table [overrides."gpt-5.1"]
table [overrides."gpt-5-nano"]
table [overrides."gpt-5-codex"]
table [overrides."gpt-5-mini"]
table [overrides."gpt-5.1-codex-max"]
table [overrides."gpt-5-chat-latest"]
table [overrides."gpt-5"]
table [overrides."gpt-5-pro"]
table [overrides."gpt-5.2"]
table [overrides."gpt-5.1-chat-latest"]
table [overrides."gpt-5.2-codex"]
table [overrides."gpt-5.3-codex"]
table [overrides."gpt-5.3-codex-spark"]
table [overrides."gpt-5.4"]
table [overrides."gpt-5.4-mini"]
table [overrides."gpt-5.4-nano"]
table [overrides."gpt-5.4-pro"]
table [overrides."gpt-5.5"]
table [overrides."gpt-5.5-pro"]
```

### api\venv\Lib\site-packages\langchain_openai\embeddings\azure.py
```
class AzureOpenAIEmbeddings(OpenAIEmbeddings)
  def validate_environment() → Self
```

### api\venv\Lib\site-packages\langchain_openai\embeddings\base.py
```
class OpenAIEmbeddings(BaseModel, Embeddings) {client?, async_client?, model?, dimensions?, deployment?, openai_api_version?}
```

### api\venv\Lib\site-packages\langchain_openai\llms\azure.py
```
class AzureOpenAI(BaseOpenAI)
  def get_lc_namespace() → list[str]
  def lc_secrets() → dict[str, str]
  def is_lc_serializable() → bool
  def validate_environment() → Self
  def lc_attributes() → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_openai\llms\base.py
```
class BaseOpenAI(BaseLLM)
  def build_extra(values: dict[str, Any]) → Any
  def validate_environment() → Self
  def get_token_ids(text: str) → list[int]
  def modelname_to_contextsize(modelname: str) → int
  def max_context_size() → int
  def max_tokens_for_prompt(prompt: str) → int
class OpenAI(BaseOpenAI)
  def get_lc_namespace() → list[str]
  def is_lc_serializable() → bool
  def lc_secrets() → dict[str, str]
  def lc_attributes() → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_openai\middleware\openai_moderation.py
```
class OpenAIModerationError(RuntimeError)
class OpenAIModerationMiddleware(AgentMiddleware[AgentState[Any], Any])
```

### api\venv\Lib\site-packages\langchain_openai\tools\custom_tool.py
```
def custom_tool(*args: Any, **kwargs: Any) → Any
```

### api\venv\Lib\site-packages\langchain_ollama\_utils.py
```
def validate_model(client: Client, model_name: str) → None  # Validate that a model exists in the local Ollama instance
def parse_url_with_auth(url: str | None) → tuple[str | None, dict[str,...
def merge_auth_headers(client_kwargs: dict, auth_headers: dict[str, str] | None) → None  # Merge authentication headers into client kwargs in-place
```

### api\venv\Lib\site-packages\langchain_ollama\chat_models.py
```
class ChatOllama(BaseChatModel)
```

### api\venv\Lib\site-packages\langchain_ollama\embeddings.py
```
class OllamaEmbeddings(BaseModel, Embeddings) {model*, dimensions?, validate_model_on_init?, base_url?, client_kwargs?, async_client_kwargs?}
```

### api\venv\Lib\site-packages\langchain_ollama\llms.py
```
class OllamaLLM(BaseLLM)
```

### api\venv\Lib\site-packages\langchain_mistralai\_compat.py
```
def translate_content(message: AIMessage) → list[types.ContentBlock]  # Derive standard content blocks from a message with mistral c
def translate_content_chunk(message: AIMessageChunk) → list[types.ContentBlock]  # Derive standard content blocks from a message chunk with mis
```

### api\venv\Lib\site-packages\langchain_mistralai\chat_models.py
```
class ChatMistralAI(BaseChatModel)
  def build_extra(values: dict[str, Any]) → Any
  def iter_sse() → Iterator[dict]
  def validate_environment() → Self
  def lc_secrets() → dict[str, str]
  def is_lc_serializable() → bool
  def get_lc_namespace() → list[str]
async def acompletion_with_retry(llm: ChatMistralAI, run_manager: AsyncCallbackManagerForLLMRun | None, **kwargs: Any) → Any  # Use tenacity to retry the async completion call
```

### api\venv\Lib\site-packages\langchain_mistralai\embeddings.py
```
class DummyTokenizer
  def encode_batch(texts: list[str]) → list[list[str]]
class MistralAIEmbeddings(BaseModel, Embeddings) {client?, async_client?, mistral_api_key?, endpoint?, max_retries?, timeout?}
```

### api\venv\Lib\site-packages\langchain_aws\__init__.py
```
def setup_logging() → None
```

### api\venv\Lib\site-packages\langchain_aws\function_calling.py
```
class AnthropicTool(TypedDict)
class _AnthropicToolUse(TypedDict)
class FunctionDescription(TypedDict)
class ToolDescription(TypedDict)
class ToolsOutputParser(BaseGenerationOutputParser)
  def parse_result(result: List[Generation], *, partial: bool) → Any
def get_system_message(tools: List[AnthropicTool]) → str
def convert_to_anthropic_tool(tool: Union[Dict[str, Any], TypeBaseModel, Callable, BaseTool]) → Union[AnthropicTool, Dict]
```

### api\venv\Lib\site-packages\langchain_aws\utils.py
```
class _StaticTokenProvider
  def __init__(token: str) → None
  def load_token(**kwargs: Any) → FrozenAuthToken
class ContentHandlerBase(Generic[INPUT_TYPE, OUTPUT_TYPE])
  def transform_input(prompt: INPUT_TYPE, model_kwargs: Dict) → bytes
  def transform_output(output: bytes) → OUTPUT_TYPE
def enforce_stop_tokens(text: str, stop: List[str]) → str  # Cut off the text as soon as any stop words occur
def anthropic_tokens_supported() → bool  # Check if all requirements for Anthropic count_tokens() are m
def count_tokens_api_supported_for_model(model: str) → bool
def get_num_tokens_anthropic(text: str) → int  # Get the number of tokens in a string of text
def get_token_ids_anthropic(text: str) → List[int]  # Get the token IDs for a string of text
def create_aws_client(service_name: str, region_name: str?, credentials_profile_name: str?, aws_access_key_id: SecretStr?, aws_secret_access_key: SecretStr?, aws_session_token: SecretStr?, endpoint_url: str?, config: Any, api_key: SecretStr?) → Any
def create_aws_bedrock_runtime_client(region_name: str?, credentials_profile_name: str?, aws_access_key_id: SecretStr?, aws_secret_access_key: SecretStr?, aws_session_token: SecretStr?, endpoint_url: str?, api_key: SecretStr?) → Any
def parse_model_provider(model_id: str) → str  # Extract the provider from a Bedrock model ID
def thinking_in_params(params: dict) → bool  # Check if the thinking parameter is enabled in the request
def thinking_disabled_in_params(params: dict) → bool  # Check if thinking is explicitly disabled in the request
def thinking_on_by_default(model: str) → bool  # Check if the model runs adaptive thinking without a thinking
def thinking_forced_tool_use_unsupported(model: str) → bool  # Check if the model rejects forced tool use while thinking is
def trim_message_whitespace(messages: List[Any]) → List[Any]  # Trim trailing whitespace from final AIMessage content
```

### api\venv\Lib\site-packages\langchain_aws\agents\base.py
```
class BedrockAgentsRunnable(RunnableSerializable[Dict, OutputType])
  def validate_agent(values: dict) → Any
class BedrockInlineAgentsRunnable(RunnableSerializable[List[BaseMessage], BaseMessage])
  def validate_environment(values: Dict) → Dict
  def to_dict() → Dict[str, Any]
  def from_dict(data: Dict[str, Any]) → "BedrockInlineAgentsRunnable"
```

### api\venv\Lib\site-packages\langchain_aws\agents\types.py
```
class BedrockAgentFinish(AgentFinish)
  def is_lc_serializable() → bool
class BedrockAgentAction(AgentAction)
  def is_lc_serializable() → bool
class GuardrailConfiguration(TypedDict)
class KnowledgebaseConfiguration(TypedDict, total=False)
class InlineAgentConfiguration(TypedDict, total=False)
```

### api\venv\Lib\site-packages\langchain_aws\agents\utils.py
```
def get_boto_session(credentials_profile_name: str?, region_name: str?, endpoint_url: str?, config: Config?) → Any  # Construct the boto3 session
def parse_agent_response(response: Any) → OutputType  # Parses the raw response from Bedrock Agent
```

### api\venv\Lib\site-packages\langchain_aws\chains\graph_qa\neptune_cypher.py
```
def trim_query(query: str) → str  # Trim the query to only include Cypher keywords
def extract_cypher(text: str) → str  # Extract Cypher code from text using Regex
def use_simple_prompt(llm: BaseLanguageModel) → bool  # Decides whether to use the simple prompt
def get_prompt(llm: BaseLanguageModel) → BasePromptTemplate  # Selects the final prompt
def create_neptune_opencypher_qa_chain(llm: BaseLanguageModel, graph: BaseNeptuneGraph, qa_prompt: BasePromptTemplate, cypher_prompt: BasePromptTemplate?, return_intermediate_steps: bool, return_direct: bool, extra_instructions: str?, allow_dangerous_requests: bool) → Runnable
```

### api\venv\Lib\site-packages\langchain_aws\chains\graph_qa\neptune_sparql.py
```
def extract_sparql(query: str) → str  # Extract SPARQL code from a text
def get_prompt(examples: str) → BasePromptTemplate  # Selects the final prompt
def create_neptune_sparql_qa_chain(llm: BaseLanguageModel, graph: NeptuneRdfGraph, qa_prompt: BasePromptTemplate, sparql_prompt: BasePromptTemplate?, return_intermediate_steps: bool, return_direct: bool, extra_instructions: str?, allow_dangerous_requests: bool, examples: str?) → Runnable[Any, dict]
```

### api\venv\Lib\site-packages\langchain_aws\chat_models\anthropic.py
```
class ChatAnthropicBedrock(ChatAnthropic)
  def lc_secrets() → dict[str, str]
  def get_lc_namespace() → list[str]
```

### api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock.py
```
class ChatPromptAdapter
class ChatBedrock(BaseChatModel, BedrockBase)
  def is_lc_serializable() → bool
  def get_lc_namespace() → List[str]
  def set_beta_use_converse_api(values: Dict) → Any
  def build_extra(values: dict[str, Any]) → Any
  def lc_attributes() → Dict[str, Any]
  def get_num_tokens(text: str) → int
  def get_token_ids(text: str) → List[int]
  def set_system_prompt_with_tools(xml_tools_system_prompt: str) → None
def convert_messages_to_prompt_llama(messages: List[BaseMessage]) → str  # Convert a list of messages to a prompt for llama
def convert_messages_to_prompt_llama3(messages: List[BaseMessage]) → str  # Convert a list of messages to a prompt for Llama 3
def convert_messages_to_prompt_llama4(messages: List[BaseMessage]) → str  # Convert a list of messages to a prompt for Llama 4
def convert_messages_to_prompt_anthropic(messages: List[BaseMessage], *, human_prompt: str, ai_prompt: str) → str  # Format a list of messages into a full prompt for the Anthrop
def convert_messages_to_prompt_mistral(messages: List[BaseMessage]) → str  # Convert a list of messages to a prompt for mistral
def convert_messages_to_prompt_deepseek(messages: List[BaseMessage]) → str  # Convert a list of messages to a prompt for DeepSeek-R1
def convert_messages_to_prompt_writer(messages: List[BaseMessage]) → str  # Convert a list of messages to a prompt for Writer
def convert_messages_to_prompt_qwen(messages: List[BaseMessage]) → str  # Convert a list of messages to a ChatML prompt for Qwen model
def convert_messages_to_prompt_openai(messages: List[BaseMessage]) → str  # Convert a list of messages to a Harmony format prompt for Op
```

### api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_converse.py
```
class ChatBedrockConverse(BaseChatModel)
  def create_cache_point(cache_type: str) → Dict[str, Any]
  def build_extra(values: dict[str, Any]) → Any
  def set_disable_streaming(values: Dict) → Any
  def validate_environment() → Self
  def is_lc_serializable() → bool
  def get_lc_namespace() → list[str]
  def lc_secrets() → Dict[str, str]
```

### api\venv\Lib\site-packages\langchain_aws\chat_models\bedrock_nova_sonic.py
```
class NovaSonicSession
  def is_active() → bool
  async def start() → None
  async def send_text(text: str, *, interactive: bool) → None
  async def start_audio_input() → None
  async def send_audio_chunk(audio_bytes: bytes) → None
  async def end_audio_input() → None
  async def receive_events() → AsyncIterator[Dict[str, Any]]
  async def close_input() → None
class ChatBedrockNovaSonic(BaseChatModel)
  def validate_environment() → Self
  def is_lc_serializable() → bool
  def get_lc_namespace() → list[str]
  def lc_secrets() → Dict[str, str]
```

### api\venv\Lib\site-packages\langchain_aws\chat_models\sagemaker_endpoint.py
```
class ChatLineIterator
  def __init__(stream: Any) → None
class ChatModelContentHandler(ContentHandlerBase[Any, Any])
class ChatSagemakerEndpoint(BaseChatModel)
  def validate_environment() → Self
  def get_lc_namespace() → List[str]
  def lc_attributes() → Dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_aws\data\profile_augmentations.toml
```
table [overrides]
key provider
key image_url_inputs
key pdf_inputs
key pdf_tool_message
key image_tool_message
```

### api\venv\Lib\site-packages\langchain_aws\document_compressors\rerank.py
```
class BedrockRerank(BaseDocumentCompressor)
  def initialize_client(values: Dict[str, Any]) → Any
```

### api\venv\Lib\site-packages\langchain_aws\embeddings\bedrock.py
```
class BedrockEmbeddings(BaseModel, Embeddings) {client?, region_name?, credentials_profile_name?, aws_access_key_id?, aws_secret_access_key?, aws_session_token?}
```

### api\venv\Lib\site-packages\langchain_aws\graphs\neptune_graph.py
```
class NeptuneQueryException(Exception)
  def __init__(exception: Union[str, Dict])
  def get_message() → str
  def get_details() → Any
class BaseNeptuneGraph(ABC)
  def get_schema() → str
  def query(query: str, params: dict) → List[Dict[str, Any]]
class NeptuneAnalyticsGraph(BaseNeptuneGraph)
  def query(query: str, params: dict) → List[Dict[str, Any]]
class NeptuneGraph(BaseNeptuneGraph)
  def query(query: str, params: dict) → List[Dict[str, Any]]
```

### api\venv\Lib\site-packages\langchain_aws\graphs\neptune_rdf_graph.py
```
class NeptuneRdfGraph
  def get_schema() → str
  def get_schema_elements() → Dict[str, Any]
  def get_summary() → Dict[str, Any]
  def load_schema(schema_elements: Dict[str, Any]) → None
```

### api\venv\Lib\site-packages\langchain_aws\llms\bedrock.py
```
class AnthropicTool(TypedDict)
class LLMInputOutputAdapter
  def prepare_output(provider: str, response: Any) → dict
class BedrockBase(BaseLanguageModel, ABC)
  def lc_secrets() → Dict[str, str]
  def validate_environment() → Self
class BedrockLLM(LLM, BedrockBase)
  def validate_environment_llm() → Self
  def is_lc_serializable() → bool
  def get_lc_namespace() → List[str]
  def lc_attributes() → Dict[str, Any]
  def get_num_tokens(text: str) → int
  def get_token_ids(text: str) → List[int]
def extract_tool_calls(content: List[dict]) → List[ToolCall]
```

### api\venv\Lib\site-packages\langchain_aws\llms\sagemaker_endpoint.py
```
class LineIterator
  def __init__(stream: Any) → None
class LLMContentHandler(ContentHandlerBase[str, str])
class SagemakerEndpoint(LLM)
  def validate_environment() → Self
def enforce_stop_tokens(text: str, stop: List[str]) → str  # Cut off the text as soon as any stop words occur
```

### api\venv\Lib\site-packages\langchain_aws\middleware\prompt_caching.py
```
class BedrockPromptCachingMiddleware(AgentMiddleware)
```

### api\venv\Lib\site-packages\langchain_aws\retrievers\bedrock.py
```
class SearchFilter(BaseModel) {andAll?, orAll?, equals?, greaterThan?, greaterThanOrEquals?, in_?}
class VectorSearchConfig(BaseModel, extra="allow") {numberOfResults?, filter?, overrideSearchType?}
class ManagedSearchConfig(BaseModel, extra="allow") {numberOfResults?, filter?, rerankingModelType?, rerankingConfiguration?}
class RetrievalConfig(BaseModel, extra="allow") {vectorSearchConfiguration?, managedSearchConfiguration?, nextToken?}
class AmazonKnowledgeBasesRetriever(BaseRetriever)
  def create_client(values: Dict[str, Any]) → Any
def agentic_retrieve(knowledge_base_id: str, query: str, *, generate_response: bool, number_of_results: int, region_name: str?, credentials_profile_name: str?) → Dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_aws\retrievers\kendra.py
```
class Highlight(BaseModel, extra="allow") {BeginOffset*, EndOffset*, TopAnswer?, Type?}
class TextWithHighLights(BaseModel, extra="allow") {Text*, Highlights?}
class AdditionalResultAttributeValue(BaseModel, extra="allow") {TextWithHighlightsValue*}
class AdditionalResultAttribute(BaseModel, extra="allow") {Key*, ValueType*, Value*}
class DocumentAttributeValue(BaseModel, extra="allow") {DateValue?, LongValue?, StringListValue?, StringValue?}
class DocumentAttribute(BaseModel, extra="allow") {Key*, Value*}
class ResultItem(BaseModel, ABC, extra="allow") {Id?, DocumentId?, DocumentURI?, DocumentAttributes?, ScoreAttributes?}
class QueryResultItem(ResultItem)
  def get_title() → str
  def get_attribute_value() → str
  def get_excerpt() → str
  def get_additional_metadata() → dict
class RetrieveResultItem(ResultItem)
  def get_title() → str
  def get_excerpt() → str
class QueryResult(BaseModel, extra="allow") {ResultItems*}
class RetrieveResult(BaseModel, extra="allow") {QueryId*, ResultItems*}
class AmazonKendraRetriever(BaseRetriever)
  def validate_top_k(value: int) → int
  def create_client(values: Dict[str, Any]) → Any
def clean_excerpt(excerpt: str) → str  # Clean an excerpt from Kendra
def combined_text(item: "ResultItem") → str  # Combine a ResultItem title and excerpt into a single string
```

### api\venv\Lib\site-packages\langchain_aws\retrievers\s3_vectors.py
```
class AmazonS3VectorsRetriever(VectorStoreRetriever)
```

### api\venv\Lib\site-packages\langchain_aws\runnables\q_business.py
```
class AmazonQ(Runnable[Union[str, ChatPromptValue, List[ChatPromptValue]], ChatPromptValue])
  def validate_environment() → Any
```

### api\venv\Lib\site-packages\langchain_aws\tools\browser_session_manager.py
```
class BrowserSessionManager
  async def get_async_browser(thread_id: str) → AsyncBrowser
  def get_sync_browser(thread_id: str) → SyncBrowser
  async def release_async_browser(thread_id: str) → None
  def release_sync_browser(thread_id: str) → None
  async def close_async_browser(thread_id: str) → None
  def close_sync_browser(thread_id: str) → None
  async def close_all_browsers() → None
```

### api\venv\Lib\site-packages\langchain_aws\tools\browser_toolkit.py
```
class BrowserToolkit
  def get_tools() → List[BaseTool]
  def get_tools_by_name() → Dict[str, BaseTool]
  def get_active_sessions() → Dict[str, Dict[str, str]]
  async def cleanup() → None
def create_browser_toolkit(region: str, proxy_configuration: Optional[Union[ProxyConfiguration, Dict[str, Any]]], extensions: Optional[Sequence[Union[BrowserExtension, Dict[str, Any]]]], profile_configuration: Optional[Union[ProfileConfiguration, Dict[str, Any]]]) → Tuple[BrowserToolkit, List[...  # Create a BrowserToolkit with thread support
```

### api\venv\Lib\site-packages\langchain_aws\tools\browser_tools.py
```
class NavigateToolInput(BaseModel) {url?}
class ClickToolInput(BaseModel) {selector?}
class GetElementsToolInput(BaseModel) {selector?}
class ExtractTextToolInput(BaseModel) {pass*}
class ExtractHyperlinksToolInput(BaseModel) {pass*}
class NavigateBackToolInput(BaseModel) {pass*}
class CurrentWebPageToolInput(BaseModel) {pass*}
class TypeTextInput(BaseModel) {selector?, text?}
class ScreenshotInput(BaseModel) {capture_type?}
class ScrollInput(BaseModel) {direction?, amount?}
class WaitForElementInput(BaseModel) {selector?, timeout?, state?}
class ThreadAwareBaseTool(BaseTool)
  def __init__(_session_manager: BrowserSessionManager, **kwargs: Any)
  def get_thread_id(config: RunnableConfig?) → str
  async def get_async_page(thread_id: str) → Any
  def get_sync_page(thread_id: str) → Any
  async def release_async_browser(thread_id: str) → None
  def release_sync_browser(thread_id: str) → None
class ThreadAwareNavigateTool(ThreadAwareBaseTool)
class ThreadAwareClickTool(ThreadAwareBaseTool)
class ThreadAwareNavigateBackTool(ThreadAwareBaseTool)
class ThreadAwareExtractTextTool(ThreadAwareBaseTool)
class ThreadAwareExtractHyperlinksTool(ThreadAwareBaseTool)
class ThreadAwareGetElementsTool(ThreadAwareBaseTool)
class ThreadAwareCurrentWebPageTool(ThreadAwareBaseTool)
```

### api\venv\Lib\site-packages\langchain_aws\tools\code_interpreter_toolkit.py
```
class ExecuteCodeInput(BaseModel) {code?, language?, clear_context?}
class ExecuteCommandInput(BaseModel) {command?}
class ReadFilesInput(BaseModel) {paths?}
class WriteFilesInput(BaseModel) {files?}
class ListFilesInput(BaseModel) {directory_path?}
class DeleteFilesInput(BaseModel) {paths?}
class UploadFileInput(BaseModel) {path?, content?, description?}
class InstallPackagesInput(BaseModel) {packages?, upgrade?}
class CodeInterpreterToolkit
  def get_tools() → List[BaseTool]
  def get_tools_by_name() → Dict[str, BaseTool]
  async def cleanup(thread_id: str?) → None
async def create_code_interpreter_toolkit(region: str, *, code_interpreter_identifier: str?) → Tuple["CodeInterpreterToolk...
```

### api\venv\Lib\site-packages\langchain_aws\tools\nova_tools.py
```
class _EmptyInput(BaseModel) {type?, args_schema?}
class NovaSystemTool(BaseTool)
  def to_bedrock_format() → Dict[str, Any]
class NovaGroundingTool(NovaSystemTool)
class NovaCodeInterpreterTool(NovaSystemTool)
```

### api\venv\Lib\site-packages\langchain_aws\tools\utils.py
```
async def aget_current_page(browser: AsyncBrowser) → AsyncPage  # Asynchronously get the current page of the browser
def get_current_page(browser: SyncBrowser) → SyncPage  # Get the current page of the browser
def get_session_key(config: RunnableConfig?, *, checkpoint_ns_scope: Literal["full", "parent"]) → str
```

### api\venv\Lib\site-packages\langchain_aws\utilities\math.py
```
def cosine_similarity(X: Matrix, Y: Matrix) → np.ndarray  # Row-wise cosine similarity between two equal-width matrices
def cosine_similarity_top_k(X: Matrix, Y: Matrix, top_k: int?, score_threshold: float?) → Tuple[List[Tuple[int, int]]...  # Row-wise cosine similarity with optional top-k and score thr
```

### api\venv\Lib\site-packages\langchain_aws\utilities\redis.py
```
class TokenEscaper
  def __init__(escape_chars_re: Pattern?)
  def escape(value: str) → str
  def escape_symbol(match: re.Match) → str
def get_client(redis_url: str, **kwargs: Any) → RedisType
```

### api\venv\Lib\site-packages\langchain_aws\utilities\utils.py
```
class DistanceStrategy(str, Enum)
def maximal_marginal_relevance(query_embedding: np.ndarray, embedding_list: list, lambda_mult: float, k: int) → List[int]  # Calculate maximal marginal relevance
```

### api\venv\Lib\site-packages\langchain_aws\utilities\valkey.py
```
def get_client(valkey_url: str, cluster_mode: bool?, **kwargs: Any) → GlideClientType
```

### api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\base.py
```
class InMemoryVectorStore(VectorStore)
  def embeddings() → Embeddings|None
  def schema() → Dict[str, List[Any]]
  def write_schema(path: Union[str, os.PathLike]) → None
  def as_retriever(**kwargs: Any) → InMemoryVectorStoreRetriever
class InMemoryVectorStoreRetriever(VectorStoreRetriever)
  def add_documents(documents: List[Document], **kwargs: Any) → List[str]
def check_index_exists(client: InMemoryDBType, index_name: str) → bool  # Check if MemoryDB index exists
```

### api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\cache.py
```
class InMemorySemanticCache(BaseCache)
  def clear(**kwargs: Any) → None
  def lookup(prompt: str, llm_string: str) → RETURN_VAL_TYPE|None
  def update(prompt: str, llm_string: str, return_val: RETURN_VAL_TYPE) → None
```

### api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\filters.py
```
class InMemoryDBFilterOperator(Enum)
class InMemoryDBFilter
  def text(field: str) → "InMemoryDBText"
  def num(field: str) → "InMemoryDBNum"
  def tag(field: str) → "InMemoryDBTag"
class InMemoryDBFilterField
  def __init__(field: str)
  def equals(other: "InMemoryDBFilterField") → bool
class InMemoryDBTag(InMemoryDBFilterField)
  def __init__(field: str)
class InMemoryDBNum(InMemoryDBFilterField)
class InMemoryDBText(InMemoryDBFilterField)
class InMemoryDBFilterExpression
def check_operator_misuse(func: Callable) → Callable  # Decorator to check for misuse of equality operators
```

### api\venv\Lib\site-packages\langchain_aws\vectorstores\inmemorydb\schema.py
```
class InMemoryDBDistanceMetric(str, Enum)
class InMemoryDBField(BaseModel) {name?}
class TextFieldSchema(InMemoryDBField)
  def as_field() → TextField
class TagFieldSchema(InMemoryDBField)
  def as_field() → TagField
class NumericFieldSchema(InMemoryDBField)
  def as_field() → NumericField
class InMemoryDBVectorField(InMemoryDBField)
  def uppercase_strings(v: str) → str
  def uppercase_and_check_dtype(v: str) → str
class FlatVectorField(InMemoryDBVectorField)
  def as_field() → VectorField
class HNSWVectorField(InMemoryDBVectorField)
  def as_field() → VectorField
class InMemoryDBModel(BaseModel) {text?, tag?, numeric?, extra?, vector?, content_key?}
def read_schema(index_schema: Optional[Union[Dict[str, List[Any]], str, os.PathLike]]) → Dict[str, Any]  # Read in the index schema from a dict or yaml file
```

### api\venv\Lib\site-packages\langchain_aws\vectorstores\s3_vectors\base.py
```
class AmazonS3Vectors(VectorStore)
  def embeddings() → Embeddings|None
  def as_retriever(**kwargs: Any) → AmazonS3VectorsRetriever
```

### api\venv\Lib\site-packages\langchain_aws\vectorstores\valkey\base.py
```
class ValkeyVectorStore(VectorStore)
  def embeddings() → Embeddings|None
  def delete(ids: List[str?], **kwargs: Any) → bool|None
def check_index_exists(client: GlideClientType, index_name: str) → bool  # Check if Valkey index exists
```

### api\venv\Lib\site-packages\langgraph\prebuilt\_tool_call_stream.py
```
class ToolCallStream
  def output_deltas() → StreamChannel[Any]
```

### api\venv\Lib\site-packages\langgraph\prebuilt\_tool_call_transformer.py
```
class ToolCallTransformer(StreamTransformer)
  def init() → dict[str, Any]
  def process(event: ProtocolEvent) → bool
  def finalize() → None
  def fail(err: BaseException) → None
```

### api\venv\Lib\site-packages\langgraph\prebuilt\chat_agent_executor.py
```
class AgentState(TypedDict)
class AgentStatePydantic(BaseModel) {messages*, remaining_steps?}
def create_react_agent(model: str
    | LanguageModelLike
    | Callable[[StateSchema, Runtime[ContextT]], BaseChatModel]
    | Callable[[StateSchema, Runtime[ContextT]], Awaitable[BaseChatModel]]
    | Callable[
        [StateSchema, Runtime[ContextT]], Runnable[LanguageModelInput, BaseMessage]
    ]
    | Callable[
        [StateSchema, Runtime[ContextT]], Awaitable[Runnable[LanguageModelInput, BaseMessage]], ], tools: Sequence[BaseTool | Callable | dict[str, Any]] | ToolNode, *, prompt: Prompt | None, response_format: StructuredResponseSchema
    | tuple[str, StructuredResponseSchema]
    | None, pre_model_hook: RunnableLike | None, post_model_hook: RunnableLike | None, state_schema: StateSchemaType | None, context_schema: type[Any] | None, checkpointer: Checkpointer | None, store: BaseStore | None, interrupt_before: list[str] | None, interrupt_after: list[str] | None, debug: bool, version: Literal["v1", "v2"], name: str | None, **deprecated_kwargs: Any) → CompiledStateGraph
```

### api\venv\Lib\site-packages\langgraph\prebuilt\interrupt.py
```
class HumanInterruptConfig(TypedDict)
class ActionRequest(TypedDict)
class HumanInterrupt(TypedDict)
class HumanResponse(TypedDict)
```

### api\venv\Lib\site-packages\langgraph\prebuilt\tool_node.py
```
class _ToolCallRequestOverrides(TypedDict, total=False)
@dataclass ToolCallRequest(tool_call, tool, state, runtime)
class ToolCallWithContext(TypedDict)
class ToolInvocationError(ToolException)
@dataclass _InjectedArgs(state, store, runtime, all_injected_keys, _optional_state_args)
class ToolNode(RunnableCallable)
  def tools_by_name() → dict[str, BaseTool]
  def execute(req: ToolCallRequest) → ToolMessage | Command
  async def execute(req: ToolCallRequest) → ToolMessage | Command
@dataclass ToolRuntime(state, context, config, stream_writer, tool_call_id, store, tools, execution_info?, server_info?)
class InjectedState(InjectedToolArg)
  def __init__(field: str | None) → None
class InjectedStore(InjectedToolArg)
def msg_content_output(output: Any) → str | list[dict]  # Convert tool output to `ToolMessage` content format
def tools_condition(state: list[AnyMessage] | dict[str, Any] | BaseModel, messages_key: str) → Literal["tools", "__end__"]
```

### api\venv\Lib\site-packages\langgraph\prebuilt\tool_validator.py
```
class ValidationNode(RunnableCallable)
  def run_one(call: ToolCall) → ToolMessage
```

### api\venv\Lib\site-packages\langgraph_prebuilt-1.1.0.dist-info\METADATA
```
def search(query: str):
def search(query: str):
class SelectNumber(BaseModel):
def a_must_be_meaningful(cls, v):
def my_graph_function():
```

### api\venv\Lib\site-packages\langchain_classic\base_memory.py
```
class BaseMemory(Serializable, ABC)
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  async def aload_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
  async def aclear() → None
```

### api\venv\Lib\site-packages\langchain_classic\env.py
```
def get_runtime_environment() → dict  # Get information about the LangChain runtime environment
```

### api\venv\Lib\site-packages\langchain_classic\hub.py
```
def push(repo_full_name: str, object: Any, *, api_url: str | None, api_key: str | None, parent_commit_hash: str, new_repo_is_public: bool, new_repo_description: str | None, readme: str | None, tags: Sequence[str] | None) → str
def pull(owner_repo_commit: str, *, include_model: bool | None, api_url: str | None, api_key: str | None) → Any
```

### api\venv\Lib\site-packages\langchain_classic\model_laboratory.py
```
class ModelLaboratory
  def __init__(chains: Sequence[Chain], names: list[str] | None)
  def compare(text: str) → None
```

### api\venv\Lib\site-packages\langchain_classic\_api\interactive_env.py
```
def is_interactive_env() → bool  # Determine if running within IPython or Jupyter
```

### api\venv\Lib\site-packages\langchain_classic\_api\module_import.py
```
def create_importer(package: str, *, module_lookup: dict[str, str] | None, deprecated_lookups: dict[str, str] | None, fallback_module: str | None) → Callable[[str], Any]
```

### api\venv\Lib\site-packages\langchain_classic\agents\agent.py
```
class BaseSingleActionAgent(BaseModel)
  def return_values() → list[str]
  def get_allowed_tools() → list[str] | None
  def input_keys() → list[str]
  def dict(**kwargs: Any) → builtins.dict
  def save(file_path: Path | str) → None
  def tool_run_logging_kwargs() → builtins.dict
class BaseMultiActionAgent(BaseModel)
  def return_values() → list[str]
  def get_allowed_tools() → list[str] | None
  def input_keys() → list[str]
  def dict(**kwargs: Any) → builtins.dict
  def save(file_path: Path | str) → None
  def tool_run_logging_kwargs() → builtins.dict
class AgentOutputParser(BaseOutputParser[AgentAction | AgentFinish])
  def parse(text: str) → AgentAction | AgentFinish
class MultiActionAgentOutputParser(BaseOutputParser[list[AgentAction] | AgentFinish],)
  def parse(text: str) → list[AgentAction] | AgentFinis
class RunnableAgent(BaseSingleActionAgent)
  def return_values() → list[str]
  def input_keys() → list[str]
class RunnableMultiActionAgent(BaseMultiActionAgent)
  def return_values() → list[str]
  def input_keys() → list[str]
class LLMSingleActionAgent(BaseSingleActionAgent)
```

### api\venv\Lib\site-packages\langchain_classic\agents\agent_iterator.py
```
class AgentExecutorIterator
  def inputs() → dict[str, str]
  def inputs(inputs: Any) → None
  def agent_executor() → AgentExecutor
  def agent_executor(agent_executor: AgentExecutor) → None
  def name_to_tool_map() → dict[str, BaseTool]
  def color_mapping() → dict[str, str]
  def reset() → None
  def update_iterations() → None
```

### api\venv\Lib\site-packages\langchain_classic\agents\agent_types.py
```
class AgentType(str, Enum)
```

### api\venv\Lib\site-packages\langchain_classic\agents\initialize.py
```
def initialize_agent(tools: Sequence[BaseTool], llm: BaseLanguageModel, agent: AgentType | None, callback_manager: BaseCallbackManager | None, agent_path: str | None, agent_kwargs: dict | None, *, tags: Sequence[str] | None, **kwargs: Any) → AgentExecutor
```

### api\venv\Lib\site-packages\langchain_classic\agents\loading.py
```
def load_agent_from_config(config: dict, llm: BaseLanguageModel | None, tools: list[Tool] | None, **kwargs: Any) → BaseSingleActionAgent | Bas...  # Load agent from Config Dict
def load_agent(path: str | Path, **kwargs: Any) → BaseSingleActionAgent | Bas...  # Unified method for loading an agent from LangChainHub or loc
```

### api\venv\Lib\site-packages\langchain_classic\agents\schema.py
```
class AgentScratchPadChatPromptTemplate(ChatPromptTemplate)
  def is_lc_serializable() → bool
```

### api\venv\Lib\site-packages\langchain_classic\agents\tools.py
```
class InvalidTool(BaseTool)
```

### api\venv\Lib\site-packages\langchain_classic\agents\utils.py
```
def validate_tools_single_input(class_name: str, tools: Sequence[BaseTool]) → None  # Validate tools for single input
```

### api\venv\Lib\site-packages\langchain_classic\agents\chat\base.py
```
class ChatAgent(Agent)
  def observation_prefix() → str
  def llm_prefix() → str
```

### api\venv\Lib\site-packages\langchain_classic\agents\chat\output_parser.py
```
class ChatOutputParser(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\conversational\base.py
```
class ConversationalAgent(Agent)
  def observation_prefix() → str
  def llm_prefix() → str
```

### api\venv\Lib\site-packages\langchain_classic\agents\conversational\output_parser.py
```
class ConvoOutputParser(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\conversational_chat\base.py
```
class ConversationalChatAgent(Agent)
  def observation_prefix() → str
  def llm_prefix() → str
```

### api\venv\Lib\site-packages\langchain_classic\agents\conversational_chat\output_parser.py
```
class ConvoOutputParser(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\log.py
```
def format_log_to_str(intermediate_steps: list[tuple[AgentAction, str]], observation_prefix: str, llm_prefix: str) → str  # Construct the scratchpad that lets the agent continue its th
```

### api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\log_to_messages.py
```
def format_log_to_messages(intermediate_steps: list[tuple[AgentAction, str]], template_tool_response: str) → list[BaseMessage]  # Construct the scratchpad that lets the agent continue its th
```

### api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\openai_functions.py
```
def format_to_openai_function_messages(intermediate_steps: Sequence[tuple[AgentAction, str]]) → list[BaseMessage]  # Convert (AgentAction, tool output) tuples into FunctionMessa
```

### api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\tools.py
```
def format_to_tool_messages(intermediate_steps: Sequence[tuple[AgentAction, str]]) → list[BaseMessage]  # Convert (AgentAction, tool output) tuples into `ToolMessage`
```

### api\venv\Lib\site-packages\langchain_classic\agents\format_scratchpad\xml.py
```
def format_xml(intermediate_steps: list[tuple[AgentAction, str]], *, escape_format: Literal["minimal"] | None) → str  # Format the intermediate steps as XML
```

### api\venv\Lib\site-packages\langchain_classic\agents\json_chat\base.py
```
def create_json_chat_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: ChatPromptTemplate, stop_sequence: bool | list[str], tools_renderer: ToolsRenderer, template_tool_response: str) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\agents\mrkl\base.py
```
class ChainConfig(NamedTuple)
class ZeroShotAgent(Agent)
  def observation_prefix() → str
  def llm_prefix() → str
class MRKLChain(AgentExecutor)
```

### api\venv\Lib\site-packages\langchain_classic\agents\mrkl\output_parser.py
```
class MRKLOutputParser(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\openai_assistant\base.py
```
class OpenAIAssistantFinish(AgentFinish)
  def is_lc_serializable() → bool
class OpenAIAssistantAction(AgentAction)
  def is_lc_serializable() → bool
class OpenAIAssistantRunnable(RunnableSerializable[dict, OutputType])
```

### api\venv\Lib\site-packages\langchain_classic\agents\openai_functions_agent\agent_token_buffer_memory.py
```
class AgentTokenBufferMemory(BaseChatMemory)
  def buffer() → list[BaseMessage]
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  def save_context(inputs: dict[str, Any], outputs: dict[str, Any]) → None
```

### api\venv\Lib\site-packages\langchain_classic\agents\openai_functions_agent\base.py
```
class OpenAIFunctionsAgent(BaseSingleActionAgent)
  def get_allowed_tools() → list[str]
  def validate_prompt() → Self
  def input_keys() → list[str]
  def functions() → list[dict]
def create_openai_functions_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: ChatPromptTemplate) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\agents\openai_functions_multi_agent\base.py
```
class OpenAIMultiFunctionsAgent(BaseMultiActionAgent)
  def get_allowed_tools() → list[str]
  def input_keys() → list[str]
  def functions() → list[dict]
```

### api\venv\Lib\site-packages\langchain_classic\agents\openai_tools\base.py
```
def create_openai_tools_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: ChatPromptTemplate, strict: bool | None) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\json.py
```
class JSONAgentOutputParser(AgentOutputParser)
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\openai_functions.py
```
class OpenAIFunctionsAgentOutputParser(AgentOutputParser)
  def parse_ai_message(message: BaseMessage) → AgentAction | AgentFinish
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\openai_tools.py
```
class OpenAIToolsAgentOutputParser(MultiActionAgentOutputParser)
  def parse(text: str) → list[AgentAction] | AgentFinis
def parse_ai_message_to_openai_tool_action(message: BaseMessage) → list[AgentAction] | AgentFi...  # Parse an AI message potentially containing tool_calls
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\react_json_single_input.py
```
class ReActJsonSingleInputOutputParser(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\react_single_input.py
```
class ReActSingleInputOutputParser(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\self_ask.py
```
class SelfAskOutputParser(AgentOutputParser)
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\tools.py
```
class ToolAgentAction(AgentActionMessageLog)
class ToolsAgentOutputParser(MultiActionAgentOutputParser)
  def parse(text: str) → list[AgentAction] | AgentFinis
def parse_ai_message_to_tool_action(message: BaseMessage) → list[AgentAction] | AgentFi...  # Parse an AI message potentially containing tool_calls
```

### api\venv\Lib\site-packages\langchain_classic\agents\output_parsers\xml.py
```
class XMLAgentOutputParser(AgentOutputParser)
  def parse(text: str) → AgentAction | AgentFinish
  def get_format_instructions() → str
```

### api\venv\Lib\site-packages\langchain_classic\agents\react\agent.py
```
def create_react_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: BasePromptTemplate, output_parser: AgentOutputParser | None, tools_renderer: ToolsRenderer, *, stop_sequence: bool | list[str]) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\agents\react\base.py
```
class ReActDocstoreAgent(Agent)
  def create_prompt(tools: Sequence[BaseTool]) → BasePromptTemplate
  def observation_prefix() → str
  def llm_prefix() → str
class DocstoreExplorer
  def __init__(docstore: Docstore)
  def search(term: str) → str
  def lookup(term: str) → str
class ReActTextWorldAgent(ReActDocstoreAgent)
  def create_prompt(tools: Sequence[BaseTool]) → BasePromptTemplate
class ReActChain(AgentExecutor)
  def __init__(llm: BaseLanguageModel, docstore: Docstore, **kwargs: Any)
```

### api\venv\Lib\site-packages\langchain_classic\agents\react\output_parser.py
```
class ReActOutputParser(AgentOutputParser)
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\self_ask_with_search\base.py
```
class SelfAskWithSearchAgent(Agent)
  def create_prompt(tools: Sequence[BaseTool]) → BasePromptTemplate
  def observation_prefix() → str
  def llm_prefix() → str
class SelfAskWithSearchChain(AgentExecutor)
def create_self_ask_with_search_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: BasePromptTemplate) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\agents\structured_chat\base.py
```
class StructuredChatAgent(Agent)
  def observation_prefix() → str
  def llm_prefix() → str
def create_structured_chat_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: ChatPromptTemplate, tools_renderer: ToolsRenderer, *, stop_sequence: bool | list[str]) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\agents\structured_chat\output_parser.py
```
class StructuredChatOutputParser(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
class StructuredChatOutputParserWithRetries(AgentOutputParser)
  def get_format_instructions() → str
  def parse(text: str) → AgentAction | AgentFinish
```

### api\venv\Lib\site-packages\langchain_classic\agents\tool_calling_agent\base.py
```
def create_tool_calling_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: ChatPromptTemplate, *, message_formatter: MessageFormatter) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\agents\xml\base.py
```
class XMLAgent(BaseSingleActionAgent)
  def input_keys() → list[str]
  def get_default_prompt() → ChatPromptTemplate
  def get_default_output_parser() → XMLAgentOutputParser
def create_xml_agent(llm: BaseLanguageModel, tools: Sequence[BaseTool], prompt: BasePromptTemplate, tools_renderer: ToolsRenderer, *, stop_sequence: bool | list[str]) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\callbacks\streaming_aiter_final_only.py
```
class AsyncFinalIteratorCallbackHandler(AsyncIteratorCallbackHandler)
  def append_to_last_tokens(token: str) → None
  def check_if_answer_reached() → bool
  async def on_llm_end(response: LLMResult, **kwargs: Any) → None
```

### api\venv\Lib\site-packages\langchain_classic\callbacks\streaming_aiter.py
```
class AsyncIteratorCallbackHandler(AsyncCallbackHandler)
  def always_verbose() → bool
  def __init__() → None
  async def on_llm_end(response: LLMResult, **kwargs: Any) → None
  async def on_llm_error(error: BaseException, **kwargs: Any) → None
  async def aiter() → AsyncIterator[str]
```

### api\venv\Lib\site-packages\langchain_classic\callbacks\streaming_stdout_final_only.py
```
class FinalStreamingStdOutCallbackHandler(StreamingStdOutCallbackHandler)
  def append_to_last_tokens(token: str) → None
  def check_if_answer_reached() → bool
```

### api\venv\Lib\site-packages\langchain_classic\callbacks\streamlit\__init__.py
```
def StreamlitCallbackHandler(parent_container: DeltaGenerator, *, max_thought_containers: int, expand_new_thoughts: bool, collapse_completed_thoughts: bool, thought_labeler: LLMThoughtLabeler | None) → BaseCallbackHandler
```

### api\venv\Lib\site-packages\langchain_classic\callbacks\tracers\logging.py
```
class LoggingCallbackHandler(FunctionCallbackHandler)
  def callback(text: str) → None
```

### api\venv\Lib\site-packages\langchain_classic\chains\base.py
```
class Chain(RunnableSerializable[dict[str, Any], dict[str, Any]], ABC)
  def raise_callback_manager_deprecation(values: dict) → Any
  def input_keys() → list[str]
  def output_keys() → list[str]
  def prep_inputs(inputs: dict[str, Any] | Any) → dict[str, str]
  async def aprep_inputs(inputs: dict[str, Any] | Any) → dict[str, str]
  def dict(**kwargs: Any) → dict
  def save(file_path: Path | str) → None
```

### api\venv\Lib\site-packages\langchain_classic\chains\example_generator.py
```
def generate_example(examples: list[dict], llm: BaseLanguageModel, prompt_template: PromptTemplate) → str  # Return another example given a list of examples for a prompt
```

### api\venv\Lib\site-packages\langchain_classic\chains\history_aware_retriever.py
```
def create_history_aware_retriever(llm: LanguageModelLike, retriever: RetrieverLike, prompt: BasePromptTemplate) → RetrieverOutputLike
```

### api\venv\Lib\site-packages\langchain_classic\chains\llm.py
```
class LLMChain(Chain)
  def is_lc_serializable() → bool
  def input_keys() → list[str]
  def output_keys() → list[str]
  def create_outputs(llm_result: LLMResult) → list[dict[str, Any]]
  def predict(callbacks: Callbacks, **kwargs: Any) → str
  async def apredict(callbacks: Callbacks, **kwargs: Any) → str
  def from_string(llm: BaseLanguageModel, template: str) → LLMChain
```

### api\venv\Lib\site-packages\langchain_classic\chains\loading.py
```
def load_chain_from_config(config: dict, **kwargs: Any) → Chain  # Load chain from Config Dict
def load_chain(path: str | Path, **kwargs: Any) → Chain  # Unified method for loading a chain from LangChainHub or loca
```

### api\venv\Lib\site-packages\langchain_classic\chains\mapreduce.py
```
class MapReduceChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\moderation.py
```
class OpenAIModerationChain(Chain)
  def validate_environment(values: dict) → Any
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\prompt_selector.py
```
class BasePromptSelector(BaseModel, ABC)
  def get_prompt(llm: BaseLanguageModel) → BasePromptTemplate
class ConditionalPromptSelector(BasePromptSelector)
  def get_prompt(llm: BaseLanguageModel) → BasePromptTemplate
def is_llm(llm: BaseLanguageModel) → bool  # Check if the language model is a LLM
def is_chat_model(llm: BaseLanguageModel) → bool  # Check if the language model is a chat model
```

### api\venv\Lib\site-packages\langchain_classic\chains\retrieval.py
```
def create_retrieval_chain(retriever: BaseRetriever | Runnable[dict, RetrieverOutput], combine_docs_chain: Runnable[dict[str, Any], str]) → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\chains\sequential.py
```
class SequentialChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
  def validate_chains(values: dict) → Any
class SimpleSequentialChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
  def validate_chains() → Self
```

### api\venv\Lib\site-packages\langchain_classic\chains\transform.py
```
class TransformChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\base.py
```
class BaseCombineDocumentsChain(Chain, ABC)
  def input_keys() → list[str]
  def output_keys() → list[str]
  def prompt_length(docs: list[Document], **kwargs: Any) → int | None
  def combine_docs(docs: list[Document], **kwargs: Any) → tuple[str, dict]
class AnalyzeDocumentChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\map_reduce.py
```
class MapReduceDocumentsChain(BaseCombineDocumentsChain)
  def output_keys() → list[str]
  def get_reduce_chain(values: dict) → Any
  def get_return_intermediate_steps(values: dict) → Any
  def get_default_document_variable_name(values: dict) → Any
  def collapse_document_chain() → BaseCombineDocumentsChain
  def combine_document_chain() → BaseCombineDocumentsChain
```

### api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\map_rerank.py
```
class MapRerankDocumentsChain(BaseCombineDocumentsChain)
  def output_keys() → list[str]
  def validate_llm_output() → Self
  def get_default_document_variable_name(values: dict) → Any
```

### api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\reduce.py
```
class CombineDocsProtocol(Protocol)
class AsyncCombineDocsProtocol(Protocol)
class ReduceDocumentsChain(BaseCombineDocumentsChain)
def split_list_of_docs(docs: list[Document], length_func: Callable, token_max: int, **kwargs: Any) → list[list[Document]]  # Split `Document` objects to subsets that each meet a cumulat
def collapse_docs(docs: list[Document], combine_document_func: CombineDocsProtocol, **kwargs: Any) → Document
async def acollapse_docs(docs: list[Document], combine_document_func: AsyncCombineDocsProtocol, **kwargs: Any) → Document
```

### api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\refine.py
```
class RefineDocumentsChain(BaseCombineDocumentsChain)
  def output_keys() → list[str]
  def get_return_intermediate_steps(values: dict) → Any
  def get_default_document_variable_name(values: dict) → Any
```

### api\venv\Lib\site-packages\langchain_classic\chains\combine_documents\stuff.py
```
class StuffDocumentsChain(BaseCombineDocumentsChain)
  def get_default_document_variable_name(values: dict) → Any
  def input_keys() → list[str]
  def prompt_length(docs: list[Document], **kwargs: Any) → int | None
def create_stuff_documents_chain(llm: LanguageModelLike, prompt: BasePromptTemplate, *, output_parser: BaseOutputParser | None, document_prompt: BasePromptTemplate | None, document_separator: str, document_variable_name: str) → Runnable[dict[str, Any], Any]
```

### api\venv\Lib\site-packages\langchain_classic\chains\constitutional_ai\base.py
```
class ConstitutionalChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\constitutional_ai\models.py
```
class ConstitutionalPrinciple(BaseModel) {critique_request*, revision_request*, name?}
```

### api\venv\Lib\site-packages\langchain_classic\chains\conversation\base.py
```
class ConversationChain(LLMChain)
  def is_lc_serializable() → bool
  def input_keys() → list[str]
  def validate_prompt_input_variables() → Self
```

### api\venv\Lib\site-packages\langchain_classic\chains\conversational_retrieval\base.py
```
class InputType(BaseModel) {question*, chat_history?}
class BaseConversationalRetrievalChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
  def save(file_path: Path | str) → None
class ConversationalRetrievalChain(BaseConversationalRetrievalChain)
class ChatVectorDBChain(BaseConversationalRetrievalChain)
```

### api\venv\Lib\site-packages\langchain_classic\chains\elasticsearch_database\base.py
```
class ElasticsearchDatabaseChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\flare\base.py
```
class QuestionGeneratorChain(LLMChain)
  def is_lc_serializable() → bool
  def input_keys() → list[str]
class FlareChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\flare\prompts.py
```
class FinishedOutputParser(BaseOutputParser[tuple[str, bool]])
  def parse(text: str) → tuple[str, bool]
```

### api\venv\Lib\site-packages\langchain_classic\chains\hyde\base.py
```
class HypotheticalDocumentEmbedder(Chain, Embeddings)
  def input_keys() → list[str]
  def output_keys() → list[str]
  def embed_documents(texts: list[str]) → list[list[float]]
  def combine_embeddings(embeddings: list[list[float]]) → list[float]
  def embed_query(text: str) → list[float]
```

### api\venv\Lib\site-packages\langchain_classic\chains\llm_checker\base.py
```
class LLMCheckerChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\llm_math\base.py
```
class LLMMathChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\llm_summarization_checker\base.py
```
class LLMSummarizationCheckerChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\natbot\base.py
```
class NatBotChain(Chain)
  def from_default(objective: str, **kwargs: Any) → NatBotChain
  def input_keys() → list[str]
  def output_keys() → list[str]
  def execute(url: str, browser_content: str) → str
```

### api\venv\Lib\site-packages\langchain_classic\chains\natbot\crawler.py
```
class ElementInViewPort(TypedDict)
class Crawler
  def __init__() → None
  def go_to_page(url: str) → None
  def scroll(direction: str) → None
  def click(id_: str | int) → None
  def type(id_: str | int, text: str) → None
  def enter() → None
  def crawl() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\base.py
```
def create_openai_fn_chain(functions: Sequence[dict[str, Any] | type[BaseModel] | Callable], llm: BaseLanguageModel, prompt: BasePromptTemplate, *, enforce_single_function_usage: bool, output_key: str, output_parser: BaseLLMOutputParser | None, **kwargs: Any) → LLMChain
def create_structured_output_chain(output_schema: dict[str, Any] | type[BaseModel], llm: BaseLanguageModel, prompt: BasePromptTemplate, *, output_key: str, output_parser: BaseLLMOutputParser | None, **kwargs: Any) → LLMChain
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\citation_fuzzy_match.py
```
class FactWithEvidence(BaseModel) {fact?, substring_quote?}
class QuestionAnswer(BaseModel) {question?, answer?}
def create_citation_fuzzy_match_runnable(llm: BaseChatModel) → Runnable
def create_citation_fuzzy_match_chain(llm: BaseLanguageModel) → LLMChain  # Create a citation fuzzy match chain
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\extraction.py
```
def create_extraction_chain(schema: dict, llm: BaseLanguageModel, prompt: BasePromptTemplate | None, tags: list[str] | None, verbose: bool) → Chain
def create_extraction_chain_pydantic(pydantic_schema: Any, llm: BaseLanguageModel, prompt: BasePromptTemplate | None, verbose: bool) → Chain
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\openapi.py
```
class SimpleRequestChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
def openapi_spec_to_openai_fn(spec: OpenAPISpec) → tuple[list[dict[str, Any]],...  # OpenAPI spec to OpenAI function JSON Schema
def get_openapi_chain(spec: OpenAPISpec | str, llm: BaseLanguageModel | None, prompt: BasePromptTemplate | None, request_chain: Chain | None, llm_chain_kwargs: dict | None, verbose: bool, headers: dict | None, params: dict | None, **kwargs: Any) → SequentialChain
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\qa_with_structure.py
```
class AnswerWithSources(BaseModel) {answer?, sources?}
def create_qa_with_structure_chain(llm: BaseLanguageModel, schema: dict | type[BaseModel], output_parser: str, prompt: PromptTemplate | ChatPromptTemplate | None, verbose: bool) → LLMChain
def create_qa_with_sources_chain(llm: BaseLanguageModel, verbose: bool, **kwargs: Any) → LLMChain
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\tagging.py
```
def create_tagging_chain(schema: dict, llm: BaseLanguageModel, prompt: ChatPromptTemplate | None, **kwargs: Any) → Chain
def create_tagging_chain_pydantic(pydantic_schema: Any, llm: BaseLanguageModel, prompt: ChatPromptTemplate | None, **kwargs: Any) → Chain
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_functions\utils.py
```
def get_llm_kwargs(function: dict) → dict  # Return the kwargs for the LLMChain constructor
```

### api\venv\Lib\site-packages\langchain_classic\chains\openai_tools\extraction.py
```
def create_extraction_chain_pydantic(pydantic_schemas: list[type[BaseModel]] | type[BaseModel], llm: BaseLanguageModel, system_message: str) → Runnable  # Creates a chain that extracts information from a passage
```

### api\venv\Lib\site-packages\langchain_classic\chains\qa_generation\base.py
```
class QAGenerationChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\base.py
```
class BaseQAWithSourcesChain(Chain, ABC)
  def input_keys() → list[str]
  def output_keys() → list[str]
  def validate_naming(values: dict) → Any
class QAWithSourcesChain(BaseQAWithSourcesChain)
  def input_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\loading.py
```
class LoadingCallable(Protocol)
def load_qa_with_sources_chain(llm: BaseLanguageModel, chain_type: str, verbose: bool | None, **kwargs: Any) → BaseCombineDocumentsChain
```

### api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\retrieval.py
```
class RetrievalQAWithSourcesChain(BaseQAWithSourcesChain)
```

### api\venv\Lib\site-packages\langchain_classic\chains\qa_with_sources\vector_db.py
```
class VectorDBQAWithSourcesChain(BaseQAWithSourcesChain)
```

### api\venv\Lib\site-packages\langchain_classic\chains\query_constructor\base.py
```
class StructuredQueryOutputParser(BaseOutputParser[StructuredQuery])
  def parse(text: str) → StructuredQuery
  def ast_parse(raw_filter: str) → FilterDirective | None
def fix_filter_directive(filter: FilterDirective | None, *, allowed_comparators: Sequence[Comparator] | None, allowed_operators: Sequence[Operator] | None, allowed_attributes: Sequence[str] | None) → FilterDirective | None
def construct_examples(input_output_pairs: Sequence[tuple[str, dict]]) → list[dict]  # Construct examples from input-output pairs
```

### api\venv\Lib\site-packages\langchain_classic\chains\query_constructor\parser.py
```
class ISO8601Date(TypedDict)
class ISO8601DateTime(TypedDict)
class QueryTransformer(Transformer)
  def program(*items: Any) → tuple
  def func_call(func_name: Any, args: list) → FilterDirective
  def args(*items: Any) → tuple
  def false() → bool
  def true() → bool
  def list(item: Any) → list
  def int(item: Any) → int
  def float(item: Any) → float
def get_parser(allowed_comparators: Sequence[Comparator] | None, allowed_operators: Sequence[Operator] | None, allowed_attributes: Sequence[str] | None) → Lark  # Return a parser for the query language
```

### api\venv\Lib\site-packages\langchain_classic\chains\query_constructor\schema.py
```
class AttributeInfo(BaseModel) {name*, description*, type*, model_config?}
```

### api\venv\Lib\site-packages\langchain_classic\chains\question_answering\chain.py
```
class LoadingCallable(Protocol)
def load_qa_chain(llm: BaseLanguageModel, chain_type: str, verbose: bool | None, callback_manager: BaseCallbackManager | None, **kwargs: Any) → BaseCombineDocumentsChain
```

### api\venv\Lib\site-packages\langchain_classic\chains\retrieval_qa\base.py
```
class BaseRetrievalQA(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
class RetrievalQA(BaseRetrievalQA)
class VectorDBQA(BaseRetrievalQA)
  def validate_search_type(values: dict) → Any
```

### api\venv\Lib\site-packages\langchain_classic\chains\router\base.py
```
class Route(NamedTuple)
class RouterChain(Chain, ABC)
  def output_keys() → list[str]
  def route(inputs: dict[str, Any], callbacks: Callbacks) → Route
class MultiRouteChain(Chain)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\router\embedding_router.py
```
class EmbeddingRouterChain(RouterChain)
  def input_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\router\llm_router.py
```
class LLMRouterChain(RouterChain)
  def input_keys() → list[str]
class RouterOutputParser(BaseOutputParser[dict[str, str]])
  def parse(text: str) → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_classic\chains\router\multi_prompt.py
```
class MultiPromptChain(MultiRouteChain)
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\router\multi_retrieval_qa.py
```
class MultiRetrievalQAChain(MultiRouteChain)
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\chains\sql_database\query.py
```
class SQLInput(TypedDict)
class SQLInputWithTables(TypedDict)
def create_sql_query_chain(llm: BaseLanguageModel, db: SQLDatabase, prompt: BasePromptTemplate | None, k: int, *, get_col_comments: bool | None) → Runnable[SQLInput | SQLInpu...
```

### api\venv\Lib\site-packages\langchain_classic\chains\structured_output\base.py
```
def create_openai_fn_runnable(functions: Sequence[dict[str, Any] | type[BaseModel] | Callable], llm: Runnable, prompt: BasePromptTemplate | None, *, enforce_single_function_usage: bool, output_parser: BaseOutputParser | BaseGenerationOutputParser | None, **llm_kwargs: Any) → Runnable
def create_structured_output_runnable(output_schema: dict[str, Any] | type[BaseModel], llm: Runnable, prompt: BasePromptTemplate | None, *, output_parser: BaseOutputParser | BaseGenerationOutputParser | None, enforce_function_usage: bool, return_single: bool, mode: Literal[
        "openai-functions", "openai-tools", "openai-json", ], **kwargs: Any) → Runnable
def get_openai_output_parser(functions: Sequence[dict[str, Any] | type[BaseModel] | Callable]) → BaseOutputParser | BaseGene...
```

### api\venv\Lib\site-packages\langchain_classic\chains\summarize\chain.py
```
class LoadingCallable(Protocol)
def load_summarize_chain(llm: BaseLanguageModel, chain_type: str, verbose: bool | None, **kwargs: Any) → BaseCombineDocumentsChain
```

### api\venv\Lib\site-packages\langchain_classic\chat_models\base.py
```
class _ConfigurableModel(Runnable[LanguageModelInput, Any])
  def queue(*args: Any, **kwargs: Any) → _ConfigurableModel
  def InputType() → TypeAlias
def init_chat_model(model: str, *, model_provider: str | None, configurable_fields: None, config_prefix: str | None, **kwargs: Any) → BaseChatModel
def init_chat_model(model: None, *, model_provider: str | None, configurable_fields: None, config_prefix: str | None, **kwargs: Any) → _ConfigurableModel
def init_chat_model(model: str | None, *, model_provider: str | None, configurable_fields: Literal["any"] | list[str] | tuple[str, ...], config_prefix: str | None, **kwargs: Any) → _ConfigurableModel
def init_chat_model(model: str | None, *, model_provider: str | None, configurable_fields: Literal["any"] | list[str] | tuple[str, ...] | None, config_prefix: str | None, **kwargs: Any) → BaseChatModel | _Configurab...
```

### api\venv\Lib\site-packages\langchain_classic\embeddings\base.py
```
def init_embeddings(model: str, *, provider: str | None, **kwargs: Any) → Embeddings | Runnable[Any, ...
```

### api\venv\Lib\site-packages\langchain_classic\embeddings\cache.py
```
class CacheBackedEmbeddings(Embeddings)
  def embed_documents(texts: list[str]) → list[list[float]]
  async def aembed_documents(texts: list[str]) → list[list[float]]
  def embed_query(text: str) → list[float]
  async def aembed_query(text: str) → list[float]
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\loading.py
```
def load_dataset(uri: str) → list[dict]  # Load a dataset from the [LangChainDatasets on HuggingFace](h
def load_evaluator(evaluator: EvaluatorType, *, llm: BaseLanguageModel | None, **kwargs: Any) → Chain | StringEvaluator  # Load the requested evaluation chain specified by a string
def load_evaluators(evaluators: Sequence[EvaluatorType], *, llm: BaseLanguageModel | None, config: dict | None, **kwargs: Any) → list[Chain | StringEvaluator]
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\schema.py
```
class EvaluatorType(str, Enum)
class LLMEvalChain(Chain)
  def from_llm(llm: BaseLanguageModel, **kwargs: Any) → LLMEvalChain
class _EvalArgsMixin
  def requires_reference() → bool
  def requires_input() → bool
class StringEvaluator(_EvalArgsMixin, ABC)
  def evaluation_name() → str
  def requires_reference() → bool
class PairwiseStringEvaluator(_EvalArgsMixin, ABC)
class AgentTrajectoryEvaluator(_EvalArgsMixin, ABC)
  def requires_input() → bool
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\agents\trajectory_eval_chain.py
```
class TrajectoryEval(TypedDict)
class TrajectoryOutputParser(BaseOutputParser)
  def parse(text: str) → TrajectoryEval
class TrajectoryEvalChain(AgentTrajectoryEvaluator, LLMEvalChain)
  def requires_reference() → bool
  def input_keys() → list[str]
  def output_keys() → list[str]
  def prep_inputs(inputs: dict[str, Any] | Any) → dict[str, str]
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\comparison\eval_chain.py
```
class PairwiseStringResultOutputParser(BaseOutputParser[dict])
  def parse(text: str) → dict[str, Any]
class PairwiseStringEvalChain(PairwiseStringEvaluator, LLMEvalChain, LLMChain)
  def is_lc_serializable() → bool
  def requires_reference() → bool
  def requires_input() → bool
class LabeledPairwiseStringEvalChain(PairwiseStringEvalChain)
  def requires_reference() → bool
def resolve_pairwise_criteria(criteria: CRITERIA_TYPE | str | list[CRITERIA_TYPE] | None) → dict  # Resolve the criteria for the pairwise evaluator
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\criteria\eval_chain.py
```
class Criteria(str, Enum)
class CriteriaResultOutputParser(BaseOutputParser[dict])
  def parse(text: str) → dict[str, Any]
class CriteriaEvalChain(StringEvaluator, LLMEvalChain, LLMChain)
  def is_lc_serializable() → bool
  def requires_reference() → bool
  def requires_input() → bool
  def evaluation_name() → str
class LabeledCriteriaEvalChain(CriteriaEvalChain)
  def is_lc_serializable() → bool
  def requires_reference() → bool
def resolve_criteria(criteria: CRITERIA_TYPE | str | None) → dict[str, str]
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\embedding_distance\base.py
```
class EmbeddingDistance(str, Enum)
class _EmbeddingDistanceChainMixin(Chain)
  def output_keys() → list[str]
class EmbeddingDistanceEvalChain(_EmbeddingDistanceChainMixin, StringEvaluator)
  def requires_reference() → bool
  def evaluation_name() → str
  def input_keys() → list[str]
class PairwiseEmbeddingDistanceEvalChain(_EmbeddingDistanceChainMixin,
    PairwiseStringEvaluator,)
  def input_keys() → list[str]
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\exact_match\base.py
```
class ExactMatchStringEvaluator(StringEvaluator)
  def requires_input() → bool
  def requires_reference() → bool
  def input_keys() → list[str]
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\parsing\base.py
```
class JsonValidityEvaluator(StringEvaluator)
  def __init__(**_: Any) → None
  def requires_input() → bool
  def requires_reference() → bool
  def evaluation_name() → str
class JsonEqualityEvaluator(StringEvaluator)
  def __init__(operator: Callable | None, **_: Any) → None
  def requires_input() → bool
  def requires_reference() → bool
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\parsing\json_distance.py
```
class JsonEditDistanceEvaluator(StringEvaluator)
  def requires_input() → bool
  def requires_reference() → bool
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\parsing\json_schema.py
```
class JsonSchemaEvaluator(StringEvaluator)
  def __init__(**_: Any) → None
  def requires_input() → bool
  def requires_reference() → bool
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\qa\eval_chain.py
```
class QAEvalChain(LLMChain, StringEvaluator, LLMEvalChain)
  def is_lc_serializable() → bool
  def evaluation_name() → str
  def requires_reference() → bool
  def requires_input() → bool
class ContextQAEvalChain(LLMChain, StringEvaluator, LLMEvalChain)
  def is_lc_serializable() → bool
  def requires_reference() → bool
  def requires_input() → bool
  def evaluation_name() → str
class CotQAEvalChain(ContextQAEvalChain)
  def is_lc_serializable() → bool
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\qa\generate_chain.py
```
class QAGenerateChain(LLMChain)
  def is_lc_serializable() → bool
  def from_llm(llm: BaseLanguageModel, **kwargs: Any) → QAGenerateChain
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\regex_match\base.py
```
class RegexMatchStringEvaluator(StringEvaluator)
  def __init__(*, flags: int, **_: Any)
  def requires_input() → bool
  def requires_reference() → bool
  def input_keys() → list[str]
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\scoring\eval_chain.py
```
class ScoreStringResultOutputParser(BaseOutputParser[dict])
  def parse(text: str) → dict[str, Any]
class ScoreStringEvalChain(StringEvaluator, LLMEvalChain, LLMChain)
  def is_lc_serializable() → bool
  def requires_reference() → bool
  def requires_input() → bool
  def evaluation_name() → str
class LabeledScoreStringEvalChain(ScoreStringEvalChain)
  def requires_reference() → bool
def resolve_criteria(criteria: CRITERIA_TYPE | str | list[CRITERIA_TYPE] | None) → dict  # Resolve the criteria for the pairwise evaluator
```

### api\venv\Lib\site-packages\langchain_classic\evaluation\string_distance\base.py
```
class StringDistance(str, Enum)
class _RapidFuzzChainMixin(Chain)
  def validate_dependencies(values: dict[str, Any]) → dict[str, Any]
  def output_keys() → list[str]
  def metric() → Callable
  def compute_metric(a: str, b: str) → float
class StringDistanceEvalChain(StringEvaluator, _RapidFuzzChainMixin)
  def requires_input() → bool
  def requires_reference() → bool
  def input_keys() → list[str]
  def evaluation_name() → str
class PairwiseStringDistanceEvalChain(PairwiseStringEvaluator, _RapidFuzzChainMixin)
  def input_keys() → list[str]
  def evaluation_name() → str
```

### api\venv\Lib\site-packages\langchain_classic\indexes\_sql_record_manager.py
```
class UpsertionRecord(Base)
class SQLRecordManager(RecordManager)
  def create_schema() → None
  async def acreate_schema() → None
  def get_time() → float
  async def aget_time() → float
  def exists(keys: Sequence[str]) → list[bool]
  async def aexists(keys: Sequence[str]) → list[bool]
  def delete_keys(keys: Sequence[str]) → None
  async def adelete_keys(keys: Sequence[str]) → None
```

### api\venv\Lib\site-packages\langchain_classic\indexes\vectorstore.py
```
class VectorStoreIndexWrapper(BaseModel) {vectorstore*, model_config?}
class VectorstoreIndexCreator(BaseModel) {vectorstore_cls?, embedding*, text_splitter?, vectorstore_kwargs?, model_config?}
```

### api\venv\Lib\site-packages\langchain_classic\llms\__init__.py
```
def get_type_to_cls_dict() → dict[str, Callable[[], type...
```

### api\venv\Lib\site-packages\langchain_classic\memory\buffer.py
```
class ConversationBufferMemory(BaseChatMemory)
  def buffer() → Any
  async def abuffer() → Any
  def buffer_as_str() → str
  async def abuffer_as_str() → str
  def buffer_as_messages() → list[BaseMessage]
  async def abuffer_as_messages() → list[BaseMessage]
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
class ConversationStringBufferMemory(BaseMemory)
  def validate_chains(values: dict) → dict
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, str]
  async def aload_memory_variables(inputs: dict[str, Any]) → dict[str, str]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
  async def aclear() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\buffer_window.py
```
class ConversationBufferWindowMemory(BaseChatMemory)
  def buffer() → str | list[BaseMessage]
  def buffer_as_str() → str
  def buffer_as_messages() → list[BaseMessage]
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_classic\memory\chat_memory.py
```
class BaseChatMemory(BaseMemory, ABC)
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
  async def aclear() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\combined.py
```
class CombinedMemory(BaseMemory)
  def check_input_key(value: list[BaseMemory]) → list[BaseMemory]
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, str]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\entity.py
```
class BaseEntityStore(BaseModel, ABC)
  def get(key: str, default: str | None) → str | None
  def set(key: str, value: str | None) → None
  def delete(key: str) → None
  def exists(key: str) → bool
  def clear() → None
class InMemoryEntityStore(BaseEntityStore)
  def get(key: str, default: str | None) → str | None
  def set(key: str, value: str | None) → None
  def delete(key: str) → None
  def exists(key: str) → bool
  def clear() → None
class UpstashRedisEntityStore(BaseEntityStore)
  def full_key_prefix() → str
  def get(key: str, default: str | None) → str | None
  def set(key: str, value: str | None) → None
  def delete(key: str) → None
  def exists(key: str) → bool
  def clear() → None
  def scan_and_delete(cursor: int) → int
class RedisEntityStore(BaseEntityStore)
  def full_key_prefix() → str
  def get(key: str, default: str | None) → str | None
  def set(key: str, value: str | None) → None
  def delete(key: str) → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\readonly.py
```
class ReadOnlySharedMemory(BaseMemory)
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, str]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\simple.py
```
class SimpleMemory(BaseMemory)
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, str]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\summary.py
```
class SummarizerMixin(BaseModel) {human_prefix?, ai_prefix?, llm*, prompt?, summary_message_cls?}
class ConversationSummaryMemory(BaseChatMemory, SummarizerMixin)
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  def validate_prompt_input_variables(values: dict) → dict
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\summary_buffer.py
```
class ConversationSummaryBufferMemory(BaseChatMemory, SummarizerMixin)
  def buffer() → str | list[BaseMessage]
  async def abuffer() → str | list[BaseMessage]
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  async def aload_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  def validate_prompt_input_variables(values: dict) → dict
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def prune() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\token_buffer.py
```
class ConversationTokenBufferMemory(BaseChatMemory)
  def buffer() → Any
  def buffer_as_str() → str
  def buffer_as_messages() → list[BaseMessage]
  def memory_variables() → list[str]
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\utils.py
```
def get_prompt_input_key(inputs: dict[str, Any], memory_variables: list[str]) → str  # Get the prompt input key
```

### api\venv\Lib\site-packages\langchain_classic\memory\vectorstore.py
```
class VectorStoreRetrieverMemory(BaseMemory)
  def memory_variables() → list[str]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def clear() → None
  async def aclear() → None
```

### api\venv\Lib\site-packages\langchain_classic\memory\vectorstore_token_buffer_memory.py
```
class ConversationVectorStoreTokenBufferMemory(ConversationTokenBufferMemory)
  def memory_retriever() → VectorStoreRetrieverMemory
  def load_memory_variables(inputs: dict[str, Any]) → dict[str, Any]
  def save_context(inputs: dict[str, Any], outputs: dict[str, str]) → None
  def save_remainder() → None
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\boolean.py
```
class BooleanOutputParser(BaseOutputParser[bool])
  def parse(text: str) → bool
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\combining.py
```
class CombiningOutputParser(BaseOutputParser[dict[str, Any]])
  def is_lc_serializable() → bool
  def validate_parsers(values: dict[str, Any]) → dict[str, Any]
  def get_format_instructions() → str
  def parse(text: str) → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\datetime.py
```
class DatetimeOutputParser(BaseOutputParser[datetime])
  def get_format_instructions() → str
  def parse(response: str) → datetime
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\enum.py
```
class EnumOutputParser(BaseOutputParser[Enum])
  def parse(response: str) → Enum
  def get_format_instructions() → str
  def OutputType() → type[Enum]
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\fix.py
```
class OutputFixingParserRetryChainInput(TypedDict, total=False)
class OutputFixingParser(BaseOutputParser[T])
  def is_lc_serializable() → bool
  def parse(completion: str) → T
  async def aparse(completion: str) → T
  def get_format_instructions() → str
  def OutputType() → type[T]
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\loading.py
```
def load_output_parser(config: dict) → dict  # Load an output parser
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\pandas_dataframe.py
```
class PandasDataFrameOutputParser(BaseOutputParser[dict[str, Any]])
  def parse(request: str) → dict[str, Any]
  def get_format_instructions() → str
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\regex.py
```
class RegexParser(BaseOutputParser[dict[str, str]])
  def is_lc_serializable() → bool
  def parse(text: str) → dict[str, str]
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\retry.py
```
class RetryOutputParserRetryChainInput(TypedDict)
class RetryWithErrorOutputParserRetryChainInput(TypedDict)
class RetryOutputParser(BaseOutputParser[T])
  def parse_with_prompt(completion: str, prompt_value: PromptValue) → T
  async def aparse_with_prompt(completion: str, prompt_value: PromptValue) → T
  def parse(completion: str) → T
  def get_format_instructions() → str
  def OutputType() → type[T]
class RetryWithErrorOutputParser(BaseOutputParser[T])
  def parse_with_prompt(completion: str, prompt_value: PromptValue) → T
  async def aparse_with_prompt(completion: str, prompt_value: PromptValue) → T
  def parse(completion: str) → T
  def get_format_instructions() → str
  def OutputType() → type[T]
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\regex_dict.py
```
class RegexDictParser(BaseOutputParser[dict[str, str]])
  def parse(text: str) → dict[str, str]
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\structured.py
```
class ResponseSchema(BaseModel) {name*, description*, type?}
class StructuredOutputParser(BaseOutputParser[dict[str, Any]])
  def parse(text: str) → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_classic\output_parsers\yaml.py
```
class YamlOutputParser(BaseOutputParser[T])
  def parse(text: str) → T
  def get_format_instructions() → str
  def OutputType() → type[T]
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\contextual_compression.py
```
class ContextualCompressionRetriever(BaseRetriever)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\ensemble.py
```
class EnsembleRetriever(BaseRetriever)
  def config_specs() → list[ConfigurableFieldSpec]
def unique_by_key(iterable: Iterable[T], key: Callable[[T], H]) → Iterator[T]  # Yield unique elements of an iterable based on a key function
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\merger_retriever.py
```
class MergerRetriever(BaseRetriever)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\multi_query.py
```
class LineListOutputParser(BaseOutputParser[list[str]])
  def parse(text: str) → list[str]
class MultiQueryRetriever(BaseRetriever)
  def unique_union(documents: list[Document]) → list[Document]
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\multi_vector.py
```
class SearchType(str, Enum)
class MultiVectorRetriever(BaseRetriever)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\parent_document_retriever.py
```
class ParentDocumentRetriever(MultiVectorRetriever)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\re_phraser.py
```
class RePhraseQueryRetriever(BaseRetriever)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\time_weighted_retriever.py
```
class TimeWeightedVectorStoreRetriever(BaseRetriever)
  def get_salient_docs(query: str) → dict[int, tuple[Document, floa
  async def aget_salient_docs(query: str) → dict[int, tuple[Document, floa
  def add_documents(documents: list[Document], **kwargs: Any) → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\base.py
```
class DocumentCompressorPipeline(BaseDocumentCompressor)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\chain_extract.py
```
class NoOutputParser(BaseOutputParser[str])
  def parse(text: str) → str
class LLMChainExtractor(BaseDocumentCompressor)
def default_get_input(query: str, doc: Document) → dict[str, Any]  # Return the compression chain input
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\chain_filter.py
```
class LLMChainFilter(BaseDocumentCompressor)
def default_get_input(query: str, doc: Document) → dict[str, Any]  # Return the compression chain input
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\cohere_rerank.py
```
class CohereRerank(BaseDocumentCompressor)
  def validate_environment(values: dict) → Any
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\cross_encoder_rerank.py
```
class CrossEncoderReranker(BaseDocumentCompressor)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\embeddings_filter.py
```
class EmbeddingsFilter(BaseDocumentCompressor)
  def validate_params(values: dict) → dict
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\document_compressors\listwise_rerank.py
```
class LLMListwiseRerank(BaseDocumentCompressor)
```

### api\venv\Lib\site-packages\langchain_classic\retrievers\self_query\base.py
```
class SelfQueryRetriever(BaseRetriever)
  def validate_translator(values: dict) → Any
  def llm_chain() → Runnable
```

### api\venv\Lib\site-packages\langchain_classic\runnables\hub.py
```
class HubRunnable(RunnableBindingBase[Input, Output])
```

### api\venv\Lib\site-packages\langchain_classic\runnables\openai_functions.py
```
class OpenAIFunction(TypedDict)
class OpenAIFunctionsRouter(RunnableBindingBase[BaseMessage, Any])
```

### api\venv\Lib\site-packages\langchain_classic\smith\evaluation\config.py
```
class EvalConfig(BaseModel) {evaluator_type*}
class SingleKeyEvalConfig(EvalConfig)
  def get_kwargs() → dict[str, Any]
class RunEvalConfig(BaseModel) {evaluators?, custom_evaluators?, batch_evaluators?, reference_key?, prediction_key?, input_key?}
```

### api\venv\Lib\site-packages\langchain_classic\smith\evaluation\name_generation.py
```
def random_name() → str  # Generate a random name
```

### api\venv\Lib\site-packages\langchain_classic\smith\evaluation\progress.py
```
class ProgressBarCallback(base_callbacks.BaseCallbackHandler)
  def increment() → None
```

### api\venv\Lib\site-packages\langchain_classic\smith\evaluation\runner_utils.py
```
class InputFormatError(Exception)
class TestResult(dict)
  def to_dataframe() → pd.DataFrame
class EvalError(dict)
  def __init__(Error: BaseException, **kwargs: Any) → None
class ChatModelInput(TypedDict)
class _RowResult(TypedDict, total=False)
@dataclass _DatasetRunContainer(client, project, wrapped_model, examples, configs, batch_evaluators?)
async def arun_on_dataset(client: Client | None, dataset_name: str, llm_or_chain_factory: MODEL_OR_CHAIN_FACTORY, *, evaluation: smith_eval.RunEvalConfig | None, dataset_version: datetime | str | None, concurrency_level: int, project_name: str | None, project_metadata: dict[str, Any] | None, verbose: bool, revision_id: str | None, **kwargs: Any) → dict[str, Any]
def run_on_dataset(client: Client | None, dataset_name: str, llm_or_chain_factory: MODEL_OR_CHAIN_FACTORY, *, evaluation: smith_eval.RunEvalConfig | None, dataset_version: datetime | str | None, concurrency_level: int, project_name: str | None, project_metadata: dict[str, Any] | None, verbose: bool, revision_id: str | None, **kwargs: Any) → dict[str, Any]
```

### api\venv\Lib\site-packages\langchain_classic\smith\evaluation\string_run_evaluator.py
```
class StringRunMapper(Serializable)
  def output_keys() → list[str]
  def map(run: Run) → dict[str, str]
class LLMStringRunMapper(StringRunMapper)
  def serialize_chat_messages(messages: list[dict] | list[list[dict]]) → str
  def serialize_inputs(inputs: dict) → str
  def serialize_outputs(outputs: dict) → str
  def map(run: Run) → dict[str, str]
class ChainStringRunMapper(StringRunMapper)
  def map(run: Run) → dict[str, str]
class ToolStringRunMapper(StringRunMapper)
  def map(run: Run) → dict[str, str]
class StringExampleMapper(Serializable)
  def output_keys() → list[str]
  def serialize_chat_messages(messages: list[dict]) → str
  def map(example: Example) → dict[str, str]
class StringRunEvaluatorChain(Chain, RunEvaluator)
  def input_keys() → list[str]
  def output_keys() → list[str]
```

### api\venv\Lib\site-packages\langchain_classic\storage\_lc_store.py
```
def create_lc_store(store: ByteStore, *, key_encoder: Callable[[str], str] | None) → BaseStore[str, Serializable]
def create_kv_docstore(store: ByteStore, *, key_encoder: Callable[[str], str] | None) → BaseStore[str, Document]  # Create a store for langchain `Document` objects from a bytes
```

### api\venv\Lib\site-packages\langchain_classic\storage\encoder_backed.py
```
class EncoderBackedStore(BaseStore[K, V])
  def mget(keys: Sequence[K]) → list[V | None]
  async def amget(keys: Sequence[K]) → list[V | None]
  def mset(key_value_pairs: Sequence[tuple[K, V]]) → None
  async def amset(key_value_pairs: Sequence[tuple[K, V]]) → None
  def mdelete(keys: Sequence[K]) → None
  async def amdelete(keys: Sequence[K]) → None
```

### api\venv\Lib\site-packages\langchain_classic\storage\file_system.py
```
class LocalFileStore(ByteStore)
  def mget(keys: Sequence[str]) → list[bytes | None]
  def mset(key_value_pairs: Sequence[tuple[str, bytes]]) → None
  def mdelete(keys: Sequence[str]) → None
  def yield_keys(*, prefix: str | None) → Iterator[str]
```

### api\venv\Lib\site-packages\langchain_classic\utils\pydantic.py
```
def get_pydantic_major_version() → int  # Get the major version of Pydantic
```

### api\venv\Lib\site-packages\feedparser_sgmllib\__init__.py
```
class SGMLParseError(RuntimeError)
class SGMLParser(_markupbase.ParserBase)
  def __init__(verbose: bool) → None
  def reset() → None
  def rawdata() → str
  def rawdata(value: str) → None
  def setnomoretags() → None
  def setliteral(*args: t.Any) → None
  def feed(data: str) → None
  def close() → None
```

### api\venv\Lib\site-packages\feedparser\api.py
```
def parse(url_file_stream_or_string, etag, modified, agent, referrer, handlers, request_headers, response_headers, resolve_relative_uris, sanitize_html)
```

### api\venv\Lib\site-packages\feedparser\encodings.py
```
def parse_content_type(line: str) → t.Tuple[str, str]  # Parse an HTTP Content-Type header
def convert_to_utf8(http_headers, data, result)  # Detect and convert the character encoding to UTF-8
```

### api\venv\Lib\site-packages\feedparser\exceptions.py
```
class ThingsNobodyCaresAboutButMe(Exception)
class CharacterEncodingOverride(ThingsNobodyCaresAboutButMe)
class CharacterEncodingUnknown(ThingsNobodyCaresAboutButMe)
class NonXMLContentType(ThingsNobodyCaresAboutButMe)
class UndeclaredNamespace(Exception)
```

### api\venv\Lib\site-packages\feedparser\html.py
```
class _BaseHTMLProcessor(sgmllib.SGMLParser)
  def __init__(encoding, _type)
  def reset()
  def parse_starttag(i)
  def feed(data)
  def normalize_attrs(attrs)
  def unknown_starttag(tag, attrs)
  def unknown_endtag(tag)
  def handle_charref(ref)
```

### api\venv\Lib\site-packages\feedparser\http.py
```
class _FeedURLHandler(urllib.request.HTTPDigestAuthHandler, urllib.request.HTTPRedirectHandler, urllib.request.HTTPDefaultErrorHandler)
  def http_error_default(req, fp, code, msg, headers)
  def http_error_301(req, fp, code, msg, hdrs)
  def http_error_401(req, fp, code, msg, headers)
def get(url, etag, modified, agent, referrer, handlers, request_headers, result)
```

### api\venv\Lib\site-packages\feedparser\mixin.py
```
class _FeedParserMixin(_base.Namespace,
        cc.Namespace,
        dc.Namespace,
        georss.Namespace,
        itunes.Namespace,
        mediarss.Namespace,
        psc.Namespace,)
  def __init__()
  def unknown_starttag(tag, attrs)
  def unknown_endtag(tag)
  def handle_charref(ref)
  def handle_entityref(ref)
  def handle_data(text, escape)
  def handle_comment(text)
  def handle_pi(text)
```

### api\venv\Lib\site-packages\feedparser\sanitizer.py
```
class _HTMLSanitizer(_BaseHTMLProcessor)
  def __init__(encoding, _type)
  def reset()
  def unknown_starttag(tag, attrs)
  def unknown_endtag(tag)
  def handle_pi(text)
  def handle_decl(text)
  def handle_data(text)
  def sanitize_style(style)
```

### api\venv\Lib\site-packages\feedparser\sgml.py
```
class _EndBracketRegEx
  def __init__()
  def search(target, index)
class EndBracketMatch
  def __init__(match)
  def start(n)
```

### api\venv\Lib\site-packages\feedparser\urls.py
```
class RelativeURIResolver(_BaseHTMLProcessor)
  def __init__(baseuri, encoding, _type)
  def resolve_uri(uri)
  def unknown_starttag(tag, attrs)
def convert_to_idn(url)  # Convert a URL to IDN notation
def make_safe_absolute_uri(base, rel)
def resolve_relative_uris(html_source, base_uri, encoding, type_)
```

### api\venv\Lib\site-packages\feedparser\util.py
```
class FeedParserDict(dict)
  def get(key, default)
  def setdefault(k, default)
```

### api\venv\Lib\site-packages\feedparser\datetimes\__init__.py
```
def registerDateHandler(func)  # Register a date handler function (takes string, returns 9-tu
```

### api\venv\Lib\site-packages\feedparser\namespaces\_base.py
```
class Namespace(object)
```

### api\venv\Lib\site-packages\feedparser\namespaces\admin.py
```
class Namespace(object)
```

### api\venv\Lib\site-packages\feedparser\namespaces\cc.py
```
class Namespace(object)
```

### api\venv\Lib\site-packages\feedparser\namespaces\dc.py
```
class Namespace(object)
```

### api\venv\Lib\site-packages\feedparser\namespaces\georss.py
```
class Namespace(object)
  def __init__()
```

### api\venv\Lib\site-packages\feedparser\namespaces\itunes.py
```
class Namespace(object)
```

### api\venv\Lib\site-packages\feedparser\namespaces\mediarss.py
```
class Namespace(object)
```

### api\venv\Lib\site-packages\feedparser\namespaces\psc.py
```
class Namespace(object)
  def __init__()
```

### api\venv\Lib\site-packages\feedparser\parsers\loose.py
```
class _LooseFeedParser(object)
  def __init__(baseuri, baselang, encoding, entities)
  def decode_entities(element, data)
  def strattrs(attrs)
```

### api\venv\Lib\site-packages\feedparser\parsers\strict.py
```
class _StrictFeedParser(object)
  def __init__(baseuri, baselang, encoding)
  def startPrefixMapping(prefix, uri)
  def startElementNS(name, qname, attrs)
  def characters(text)
  def endElementNS(name, qname)
  def error(exc)
  def fatalError(exc)
```

### api\app\services\scraper.py
```
class WebPageScraper
  def __init__()
  async def scrape(url: str) → str
```

### api\test_pipeline_nvidia.py
```
async def main()
```

### api\app\config.py
```
class Settings(BaseSettings) {supabase_url?, supabase_service_role_key?, openai_api_key?, exa_api_key?, redis_url?, reddit_client_id?}
def get_settings() → Settings
```

### api\test_scheduler.py
```
async def main()
```

### api\app\engines\collector\sources\hn.py
```
class HNSourceManager(BaseSourceManager)
  def name() → str
  async def collect(product: dict) → list[Signal]
```

### api\test_prompt_relevance.py
```
async def test_relevance()
```

### api\app\services\analyzer.py
```
class ProductAnalyzer
  def __init__()
  async def analyze(scraped_text: str) → dict
  async def analyze_screenshot(image_base64: str) → str
```

### api\app\services\db.py
```
class DBService
  def __init__()
  def save_product(product_data: dict) → dict
  def get_active_products() → list[dict]
  def get_product_by_id(product_id: str) → dict
  def get_existing_lead_urls(product_id: str) → set[str]
  def save_leads(leads: list[dict]) → list[dict]
```

### api\app\workers\scheduler.py
```
class MonitoringScheduler
  def __init__()
  async def run_single_iteration()
  def start_loop_thread(interval_seconds: int)
  def loop_target()
  async def start_loop(interval_seconds: int)
  def stop_loop()
```

### api\app\engines\collector\orchestrator.py
```
class CollectorEngine
  def __init__()
  async def collect_for_product(product: dict) → dict
```

### api\app\engines\collector\sources\devto.py
```
class DevtoCollector(BaseSourceManager)
  def name() → str
  async def collect(product: dict) → list[Signal]
```

### api\app\engines\collector\sources\exa.py
```
class ExaSourceManager(BaseSourceManager)
  def __init__()
  def name() → str
  async def collect(product: dict) → list[Signal]
def is_noise(text: str) → bool  # Check if text is noise
```

### api\app\engines\collector\sources\exa_producthunt.py
```
class ExaProductHuntCollector(BaseSourceManager)
  def name() → str
  def __init__()
  async def collect(product: dict) → list[Signal]
def is_noise(text: str) → bool  # Check if text is noise
```

### api\app\engines\collector\sources\exa_quora.py
```
class ExaQuoraCollector(BaseSourceManager)
  def name() → str
  def __init__()
  async def collect(product: dict) → list[Signal]
def is_noise(text: str) → bool  # Check if text is noise
```

### api\app\engines\collector\sources\exa_reddit.py
```
class ExaRedditCollector(BaseSourceManager)
  def name() → str
  def __init__()
  async def collect(product: dict) → list[Signal]
def is_noise(text: str) → bool  # Check if text is noise
```

### api\app\engines\collector\sources\exa_twitter.py
```
class ExaTwitterCollector(BaseSourceManager)
  def name() → str
  def __init__()
  async def collect(product: dict) → list[Signal]
def is_noise(text: str) → bool  # Check if text is noise
```

### api\app\engines\collector\sources\indiehackers.py
```
class IndieHackersCollector(BaseSourceManager)
  def name() → str
  async def collect(product: dict) → list[Signal]
```

### api\app\engines\collector\sources\lobsters.py
```
class LobstersCollector(BaseSourceManager)
  def name() → str
  async def collect(product: dict) → list[Signal]
```

### api\app\engines\collector\sources\reddit_rss.py
```
class RedditRSSCollector(BaseSourceManager)
  def name() → str
  def __init__()
  async def collect(product: dict) → list[Signal]
```

### api\app\engines\scorer\llm_scorer.py
```
class LLMIntentScorer
  def __init__()
  async def score(text: str, product_description: str, icp: dict) → dict
  async def score_batch(signals: list, product_description: str, icp: dict) → list[dict]
```

### api\app\engines\scorer\pre_filter.py
```
class SoftPreFilter
  def __init__(keywords: list[str])
  def should_keep(text: str) → bool
```

### api\app\main.py
```
class SupportMessagePayload(BaseModel) {email*, message*}
async def lifespan(app: FastAPI)
async def root()
async def support_message(payload: SupportMessagePayload)  # Log support messages submitted via the floating Contact moda
GET /  →  root()
POST /api/support  →  support_message()
```

### api\app\routes\billing.py
```
class CheckoutRequest(BaseModel) {price_id*, plan_name*}
class DemoSubscription(BaseModel) {status*, plan*, message*}
async def create_checkout(request: CheckoutRequest) → dict  # Create demo checkout session (simulates Stripe)
async def demo_subscribe(request: CheckoutRequest) → DemoSubscription  # Demo subscription endpoint
async def subscription_status() → dict  # Get subscription status (demo - always returns free tier)
POST /checkout  →  create_checkout()
POST /subscribe  →  demo_subscribe()
GET /status  →  subscription_status()
```

### api\app\routes\products.py
```
class AnalyzeRequest(BaseModel) {url*, screenshot?}
class ProductCreate(BaseModel) {url*, name?, description?, keywords?, competitor_names?, subreddit_list?}
class ProductSetupPayload(BaseModel) {user_id*, url*, name*, description*, keywords*, competitor_names?}
class ScanRequest(BaseModel) {product_id*}
async def analyze_product(req: AnalyzeRequest)  # Analyze a URL and extract product info using LLM, with optio
async def setup_product(product: ProductSetupPayload)  # Save product context to Supabase products table and run init
async def collect_and_score(product: ProductCreate)  # Collect, score, AND enrich signals
async def full_pipeline(product: ProductCreate)  # Complete pipeline: collect -> score -> enrich -> draft
async def collect_signals(product: ProductCreate)  # Collect signals for a product from all sources
async def trigger_manual_scan(req: ScanRequest)  # Triggers an immediate, synchronous monitoring scan for the g
POST /analyze  →  analyze_product()
POST /setup  →  setup_product()
POST /collect-and-score  →  collect_and_score()
POST /full-pipeline  →  full_pipeline()
POST /collect  →  collect_signals()
POST /scan  →  trigger_manual_scan()
```

### api\app\services\drafter.py
```
class OutreachDrafter
  def __init__()
  def generate_drafts(lead: dict, product: dict) → dict
```

### api\app\services\keyword_generator.py
```
class KeywordGenerator
  def is_noise(text: str) → bool
  def has_buyer_intent(text: str) → bool
```

## frontend

### frontend\src\app\globals.css
```
var --background
var --foreground
var --card
var --card-foreground
var --popover
var --popover-foreground
var --primary
var --primary-foreground
var --secondary
var --secondary-foreground
var --muted
var --muted-foreground
var --accent
var --accent-foreground
var --border
var --input
var --ring
var --radius
.bg-halftone-dots
```

### frontend\src\app\dashboard\layout.tsx
```
component DashboardLayout
hook useState
hook usePathname
hook useRouter
hook useEffect
handler onSubmit
handler onChange
```

### frontend\src\app\dashboard\home\page.tsx
```
component HomeOnboarding
hook useState
hook useRouter
handler onSubmit
handler onChange
```

### frontend\src\app\dashboard\profile\page.tsx
```
component ProfilePage
hook useState
hook useRouter
hook useEffect
handler onClick
```

### frontend\src\app\dashboard\products\[productId]\page.tsx
```
component ProductDashboard
hook useParams
hook useState
hook useEffect
handler onClick
```

### frontend\src\app\auth\login\page.tsx
```
component Login
hook useState
hook useRouter
handler onSubmit
handler onChange
```

### frontend\src\app\auth\signup\page.tsx
```
component Signup
hook useState
hook useRouter
handler onSubmit
handler onChange
```

### frontend\src\app\dashboard\leads\[leadId]\page.tsx
```
component LeadDetail
hook useParams
hook useState
hook useEffect
```

### frontend\src\app\dashboard\page.tsx
```
component WorkspacesDashboard
hook useState
hook useEffect
```

### frontend\src\app\dashboard\settings\page.tsx
```
component Settings
hook useState
hook useRouter
hook useEffect
handler onSubmit
handler onChange
handler onClick
```

### frontend\src\app\page.tsx
```
component Home
```

### frontend\src\lib\api.ts
```
export class ApiError
  constructor(public status, message,)
export async function apiGet(path)
export async function apiPost(path, data)
```

### frontend\src\lib\supabase.ts
```
export function createClient()
export async function getCurrentUser()
export async function signOut()
```

### frontend\src\middleware.ts
```
export async function middleware(request)
```
